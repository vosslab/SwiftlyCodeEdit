CodeEditLanguages is currently metadata-only.

This placeholder keeps the package resource bundle available for existing code
that reads `Bundle.module.resourceURL` while the parser resources remain outside
the required plain-editor build path.
