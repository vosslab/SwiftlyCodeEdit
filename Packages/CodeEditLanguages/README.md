# CodeEditLanguages

Language metadata and syntax-definition assets for CodeEdit.

This package provides:

- language identifiers and file-extension detection
- comment syntax metadata
- bundled syntax-definition resources

It does not contain parser runtimes or parser package fan-out.
