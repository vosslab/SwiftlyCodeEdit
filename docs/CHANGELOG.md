## 2026-07-11

### Additions and New Features

- Native macOS 26 Liquid Glass toolbar: the SwiftUI `.toolbar` (`editorToolbar()` in
  `CodeEdit/Features/Editor/Views/CodeFileView.swift`) is bridged into the AppKit-hosted document
  window via `hostingController.sceneBridgingOptions = [.toolbars, .title]`
  (`CodeFileDocumentBridge.swift`), replacing the custom ribbon with a native grouped-capsule
  toolbar (7 items) that adopts Liquid Glass automatically from the OS; see the architect
  decision at `docs/active_plans/decisions/native_toolbar_decision.md`.
- Glass-pop chrome tint landed on the status bar: `PlainEditorStatusBar` now tints its
  `glassEffect` with `Color.accentColor.opacity(0.14)` (light) / `0.20` (dark), plus a reduce-
  transparency opaque tinted fallback via `@Environment(\.accessibilityReduceTransparency)`. A
  toolbar backdrop gradient band was also attempted but is not yet realized: measured evidence
  shows the light-mode status bar pops (spread 15.9, WCAG 5.23:1) while the dark-mode status bar
  and the toolbar capsule do not yet show a visible color pop -- see the "Post-pop re-capture"
  section of `docs/active_plans/reports/wp_g2_glass_evidence_report.md` (REWORK verdict for the
  pop goal as a whole; the underlying base toolbar/status-bar Liquid Glass material itself
  remains SHIP).
- WP-G2 light and dark glass evidence captured: `docs/screenshots/glass_toolbar_light.png` /
  `glass_toolbar_dark.png`, plus backdrop-differential fixtures
  `glass_backdrop_green_light.png` / `glass_backdrop_red_light.png`, recorded in
  `docs/active_plans/reports/wp_g2_glass_evidence_report.md`.
- Finalized the native toolbar's item layout: `window.toolbarStyle = .unified`
  (`CodeFileDocumentBridge.swift`) plus a custom `ToolbarButtonLabel`
  (`CodeFileView.swift`) that lays each item's icon and text out horizontally in an
  `HStack`, since the system's default icon-above-text stacking rendered a tall band
  the Kate-style single-row layout is meant to avoid. Toolbar buttons are grouped
  under `ToolbarItemGroup(placement: .navigation)` (4 groups, 7 items total:
  New/Open, Save/Save As, Undo/Redo, Clean Text) so the row docks at the leading
  edge, right after the traffic lights, instead of the system default's trailing
  float; the native grouped-capsule Liquid Glass and each item's enabled/disabled
  state are unaffected.
- Landed a working status-bar glass-pop mechanism:
  `CodeFileWindowBridge.installWindowController` sets `window.backgroundColor` to a
  gentle accent blend (`NSColor.controlAccentColor.blended(withFraction:of:)`
  against `.windowBackgroundColor`, 4% accent in light mode / 7% in dark) resolved
  once against the effective appearance at window-creation time, giving
  `PlainEditorStatusBar`'s `.glassEffect(.regular.tint(...))`
  (`Color.accentColor.opacity(0.08)` light / `0.10` dark) a colored backdrop to
  sample; the existing reduce-transparency opaque fallback
  (`@Environment(\.accessibilityReduceTransparency)`) is unchanged. Known
  limitation: the backdrop color is resolved once at window creation and does not
  live-re-tint if the system appearance toggles while the window stays open.

### Behavior or Interface Changes

- The editor chrome (toolbar and status bar) now carries a subtle accent tint instead of plain
  neutral glass; the editor text surface itself is unchanged (`.textBackgroundColor`), per the
  glass evidence report.

### Fixes and Maintenance

- Doc-backstop: `CodeEditTests/PackageSmoke/CodeFileDocumentLifecycleGapTests.swift`'s header
  comment was stale (described Findings 2-4 as still open); updated to reflect that all four
  WP-L1..L4 findings are closed and no test carries a `withKnownIssue` wrapper. Reworded stale
  ribbon-era comments left over from the native-toolbar cutover and removed a stray
  `_temp_winid.py` scratch file.

### Removals and Deprecations

- Archived the completed milestone-3 planning artifacts: `MILESTONE3_CHECKLIST.md` and
  `docs/active_plans/active/scope_closure_plan.md` moved via `git mv` to
  `docs/archive/MILESTONE3_CHECKLIST.md` and `docs/archive/scope_closure_plan.md`. Cross-reference
  links in `AGENTS.md`, `docs/HUMAN_GUIDANCE.md`, `docs/ROADMAP.md`, and
  `docs/archive/MILESTONE3_CHECKLIST.md` itself were updated to point at the new archive paths.

### Decisions and Failures

- Toolbar color-pop is architecturally infeasible with the current bridge. Placing tinted
  color behind the native bridged toolbar was tried through a SwiftUI content-layer gradient,
  `.toolbarBackground`, `.toolbarBackgroundVisibility`, and `window.backgroundColor` (the same
  property that successfully feeds the status bar's glass-pop above), and every attempt
  measured zero tint at the toolbar band. The bridged `NSToolbar`
  (`hostingController.sceneBridgingOptions = [.toolbars, .title]`) is window-server chrome
  outside the SwiftUI paintable region, so it cannot sample color composited by the hosted
  SwiftUI content or by `NSWindow.backgroundColor`; a toolbar color pop would need a custom
  `NSToolbar`/`NSVisualEffectView` replacement, an architect-level change out of scope here.
  The toolbar therefore ships with OS Liquid Glass only, which correctly tracks light/dark
  automatically; the status bar carries the chrome's accent color instead.

### Developer Tests and Notes

- DEBUG appearance-force harness: `ForcedAppearanceOverride.applyIfRequested()`
  (`CodeFileDocumentBridge.swift`) sets `NSApp.appearance` from the `-AppleInterfaceStyle
  Light|Dark` launch argument (volatile `NSArgumentDomain`, no `defaults write`), proven by
  `APPEARANCE_MODE=light` and `=dark` runtime markers quoted in the glass evidence report.
- The WP-G2 captures used the live on-screen easy-screenshot path (not an offscreen render), so
  the display must stay awake and unlocked for the duration of the run; a sleeping or locked
  display produces a black capture.
- Added `scripts/capture_screenshots.sh`, which regenerates all 6 `docs/screenshots/` PNGs
  reproducibly from the current build: five live on-screen window captures (the representative
  default-source window, plus varied-token and backdrop-differential fixtures under
  `tests/fixtures/screenshots/`) with light/dark forced via `-AppleInterfaceStyle` and a fixed
  15pt font, each launch serialized (`pkill`/`pgrep` before and after) so no capture races a
  prior instance, plus the programmatic app-icon preview from `scripts/make_app_icon.py`.
  `scripts/check_screenshot_not_black.py` gates each capture against a near-black
  mean-brightness floor so a slept-or-locked-display capture is skipped rather than silently
  overwriting a good tracked PNG. All 6 `docs/screenshots/` PNGs were regenerated with the
  final chrome (unified toolbar layout, status-bar accent tint) using this script.

## 2026-07-10

### Additions and New Features

- WP-Q2 follow-on: the keystroke latency harness now reports status-refresh cost separately from highlight cost so a future status-subsystem regression is attributable rather than hidden inside the combined per-edit number. The chrome model logs `STATUS_REFRESH_MS=<ms>` for the synchronous cursor scan on the keystroke hot path (`CodeFileView.swift`, DEBUG only), timing the sole O(document) work left after WP-Q2; `tests/e2e/e2e_keystroke_latency.py` parses those markers, prints `status_refresh_ms: min/median/p95/count`, and records `status_refresh_*` lines in `test-results/perf/keystroke_latency.txt` without changing the gate logic (the M8 p95 gate stays on `KEYSTROKE_MS`).
- WP-Q6: shipped bounded per-keystroke rehighlight and split the plain-editor highlighter into single-concern types. `PlainSyntaxHighlighter` (`CodeEdit/Features/Editor/Views/PlainSyntaxHighlighter.swift`) is now a thin orchestrator: cold open, theme change, and reload take the whole-document `HighlightFullPass`; each keystroke takes `scheduleBoundedRehighlight`, which reinterprets only a region around the edit instead of the whole 1 MB buffer. The supporting concerns moved to sibling files -- per-storage state and the DEBUG completion seam in `PlainSyntaxHighlightState.swift` (`HighlightState`/`HighlightStateStore`, an `NSMapTable` keyed by storage object identity so one window's request never invalidates another's), region math in `PlainSyntaxHighlightRegion.swift` (`HighlightRegionPlanner`), attribute application in `PlainSyntaxHighlightPainter.swift` (`HighlightPainter`), and the whole-document scheduling loop plus viewport-first cold paint in `PlainSyntaxHighlightFullPass.swift`. Two region strategies were prototyped and measured under the WP-Q5 keystroke harness; the edited-line-window strategy ships (the edited line(s) plus 40 context lines each side, cost independent of viewport and edit position, so the just-typed text is always repainted) over the visible-range candidate (which leaves an off-screen edit stale). The single edited-range broadcast drives exactly one bounded pass per edit, removing the old double-highlight. Comparison recorded at `test-results/perf/keystroke_latency_candidates.txt`.
- Wired SwiftLint 0.65.0 into the repo: added `brew "swiftlint"` to a new root `Brewfile`, and recorded a baseline lint run at `docs/active_plans/audits/swiftlint_baseline_2026-07-10.md` (340 violations: 320 warning, 20 error, snapshotted while four other lanes were actively editing Swift sources). A `.swiftlint.yml` already existed in the repo; it was left as-is except for six added lines across two passes: four `excluded:` entries (`.build`, `Packages/*/.build`, `OTHER_REPOS`, `.tmp`) so the lint run stops scanning gitignored reference checkouts and build artifacts that are not this repo's source, plus an `identifier_name.excluded` entry for `lf` (the deliberate 2-character `LineEndingStyle` enum case name in `PlainEditorTextCleaner.swift`/`PlainEditorSettingsKeys.swift`, added after cross-checking against the user's own stock default-rule run). The task brief for this baseline assumed the repo used tabs-based indentation per `docs/SWIFT_STYLE.md`; that doc is actually a SwiftUI/AppKit architecture best-practices guide with no indentation rule, and a direct scan of tracked Swift sources found 19,579 lines of 4-space indentation versus 28 lines of tab indentation, confirming the existing config's `spaces_over_tabs` custom rule (already present, unchanged) matches the repo's real convention; `line_length` was likewise left at SwiftLint's defaults since `docs/SWIFT_STYLE.md` states no line-length number. One Swift source line was fixed as an authorized lint-infrastructure exception: `CodeEdit/Features/Theming/SyntaxTheme.swift`'s `swiftlint:disable-previous-line` directive (not a valid SwiftLint form) was corrected to `swiftlint:disable:previous`, clearing the `invalid_swiftlint_command` violation. The baseline report includes a fix-vs-disable rule shortlist, a three-way config-false-positive/mechanical-fix/structural split cross-checked against the user's own stock `swiftlint` run scoped to `CodeEdit/`, and a recommended `tests/test_swiftlint.py` gate shape for the user to decide on.
- WP-F6: replaced `PlainEditorFontSettings`'s hardcoded four-family font list with a live CoreText enumeration. New `CodeEdit/Features/Settings/PlainEditorFontEnumeration.swift` calls `CTFontManagerCopyAvailableFontFamilyNames` and filters to families whose `CTFontGetSymbolicTraits` carries `.traitMonoSpace`, so `PlainEditorFontSettings.availableFontFamilies` (consumed by `FontSettingsView`'s picker) reflects whatever fixed-pitch fonts are actually installed, and a newly installed monospace font appears without a rebuild; `defaultFontFamily` ("SF Mono") is force-included if CoreText's enumeration ever omits it, so the default always stays selectable. CoreText was chosen over `NSFontManager` because it is a pure font-data query, not an AppKit controller, so the enumeration helper can live in a Settings-feature file rather than behind the AppKit-boundary allowlist. Extended the Format menu (`CodeEdit/App/Commands/EditorCommands.swift`) with Increase Size (`Cmd+=`), Decrease Size (`Cmd+-`), and Reset Size (`Cmd+0`), each disabled at its respective size limit; all three read/write the same `PlainEditor.fontFamily`/`PlainEditor.fontSize` `@AppStorage` keys the ribbon and Settings scene already share, so a menu invocation and a ribbon click agree across every open window. Added shared pure step functions `PlainEditorFontSettings.increasedFontSize(from:)`/`decreasedFontSize(from:)`, called by both the ribbon's `A-`/`A+` buttons and the new menu items, so the two surfaces run one action function per the work package's acceptance criteria. Added `availableFontFamiliesIsNonEmptyAndFixedPitch` and `increaseAndDecreaseFontSizeStepAndClamp` to `PlainEditorFontSettingsTests.swift`. Documented the menu shortcuts and live font enumeration in `docs/USAGE.md`. Verified: `swift build` "Build complete!" zero new warnings; `swift test` 128 tests in 14 suites pass with exactly 3 known lifecycle issues (WP-L2..L4); `pytest tests/` 1974 passed. `scripts/plain_editor_smoke.sh`'s `check_menu_has_items "Format" "Font and Text Options"` gate still passes since that item is untouched and the gate tolerates extra menu items.
- WP-F5 live-apply observability seam: added `CodeEdit/Features/Settings/PlainEditorSettingsApplySelfTest.swift`, a DEBUG-only self-test gated by `CODEEDIT_SETTINGS_APPLY_SELF_TEST=1` and scheduled from `CodeFileView.onTextViewReady` (mirroring `PlainEditorCommandSelfTest`), that proves the `SETTINGS_APPLIED` markers fire from a genuine post-mount live change through the same code path the Settings window uses. The empirical finding this seam works around: a cold launch seeded via `NSArgumentDomain` cannot fire the markers -- `PlainTextEditorView.makeNSViewController` and its first `updateNSViewController` read the same `@AppStorage` font value so the creation comparison never differs, and with one bundled theme (`standard`) every requested name resolves back to that name so the theme name never changes. After the window mounts and the first highlight pass settles (a +1.0 s start, past the command self-test's +0.5 s run), the seam changes the font size and theme through `UserDefaults.standard` (the store `@AppStorage` is backed by), so every open `CodeFileView` re-renders: the new font reaches `PlainTextEditorView.updateNSViewController` (firing `SETTINGS_APPLIED key=fontSize` after the font is actually set on the live `TextView`) and the new theme name reaches `PlainSyntaxHighlighter.applyHighlight` through `CodeFileView`'s `onChange(of:)` (firing `SETTINGS_APPLIED key=theme`). The theme-name change is made observable with only one bundled theme by a new DEBUG-only in-memory theme registry on `ThemeRepository` (`registerInMemoryTheme(_:)`/`clearInMemoryThemes()`, consulted ahead of disk in `resolveThemeFromDisk`): the self-test registers a distinctly-named copy of the bundled default's variants, so a genuine resolved-name change flows through the same `resolvedTheme(named:)` path with zero disk or Themes-directory writes. The seam captures each `UserDefaults` key's prior value including absence, applies the distinct values, then restores them (removing a key that was absent before), and logs `SETTINGS_APPLY_SELF_TEST fontRestored=true themeRestored=true` -- so a green run also proves the live change reversed and left the user's stored preferences untouched. Moved the `key=theme` marker in `PlainSyntaxHighlighter.applyHighlight` to fire after the theme colors are actually applied to the storage and laid out (rather than at the resolve), so it reports a real rendered-state change per the acceptance criteria. `scripts/plain_editor_smoke.sh` now sets the env var on launch and hard-gates on `SETTINGS_APPLIED key=fontSize`, `SETTINGS_APPLIED key=theme`, and the `SETTINGS_APPLY_SELF_TEST fontRestored=true themeRestored=true` reversal line. Added a `ThemeRepositoryTests` case covering register-resolves-by-name-then-clear-falls-back. Zero behavior change without the env var; the seam and registry compile under `#if DEBUG` only. Documented the new gates in `docs/SMOKE_TEST.md` and marked the seam TODO in `docs/TODO.md` done.
- WP-G0: added a DEBUG-only in-app window self-capture seam so release-evidence and smoke screenshots no longer need the macOS screen-recording (TCC) grant, which is denied in the agent environment and made every smoke run skip the screenshot gate. Fixing the design (the app renders its OWN window) rather than the symptom (an external `screencapture`/easy-screenshot helper behind a TCC prompt): when the app is launched with `-PlainEditor.captureWindowTo <absolute path>` (read from UserDefaults' volatile, per-process NSArgumentDomain, so it activates for exactly one launch with no `defaults write`), it renders the key editor window's content view to a PNG via `NSView.bitmapImageRepForCachingDisplay` + `cacheDisplay` -- own-view rendering that needs no screen-recording grant -- and logs `WINDOW_CAPTURE_WRITTEN path=<path>`. Capture fires only after the first highlight pass has settled (`PlainSyntaxHighlighter.onHighlightSettled`, plus a short deferral so the window has finished presenting), so a blank/unpainted window is never captured; the PNG is written to a sibling temp file and swapped into place, so a failed render never truncates or deletes an existing file at the destination; any failure logs a `WINDOW_CAPTURE_FAILED reason=<...>` marker instead. New `CodeEdit/Features/Editor/Views/PlainEditorWindowCapture.swift` holds the AppKit-free half (launch-argument read and absolute-path validation, unit-tested); the NSWindow/NSView/NSBitmapImageRep rendering (`WindowCaptureScheduler`) lives in the sanctioned document-layer bridge `CodeFileDocumentBridge.swift`, the only place AppKit window/view symbols are permitted, wired in `CodeFileView.onTextViewReady` next to the existing DEBUG self-test and keystroke bench. The whole seam compiles only under `#if DEBUG`; when the argument is absent it is a no-op with zero overhead. Added `PlainEditorWindowCaptureTests.swift` (4 Swift Testing cases: absolute path resolves to a matching file URL, and nil/empty/relative arguments resolve to nil). Verified: `./build_debug.sh` "Build complete!" zero new warnings; `swift test` 123 tests in 14 suites pass with exactly 4 known issues (the WP-L1..L4 lifecycle parity contract); a scratch end-to-end runner launched the built app against a copied `/tmp` fixture, logged `WINDOW_CAPTURE_WRITTEN path=/tmp/wp_g0_capture.png` (a 1920x1200 RGBA PNG of the highlighted editor), and `python3 tests/e2e/e2e_screenshot_colors.py /tmp/wp_g0_capture.png` passed the 3-hue-family floor (`chromatic_hue_families=3`); `pytest tests/` 1968 passed. Smoke-script integration (asserting the marker and running the capture in `scripts/plain_editor_smoke.sh`) is a later patch, and appearance forcing (dark/light/reduced-transparency, WP-G1/G2) composes with this seam via additional launch arguments later.
- WP-F1 patch 18: ported the find/replace panel from `Packages/CodeEditSourceEditor/.../Find/` into `CodeEdit/Features/Find/` (four new files), behavior-first rather than copying the old package's 19-file structure. The old panel's `FindPanelViewModel` down-cast its target to `TextViewController` (`FindPanelViewModel.swift:69`) purely to read cursor positions; the port removes that dependency by defining a small `FindPanelTarget` protocol (`textView`, `cursorLocation`, `select(range:scrollToVisible:)`) satisfied directly by `TextViewFindTarget` over the app's `CodeEditTextView.TextView` via its `selectionManager`, so no controller is involved. `FindEngine.swift` is the pure search core (harvested from `FindPanelViewModel+Find`): `findMatches(in:query:method:matchCase:)` returns `Result<[NSRange], FindError>`, escaping the query for the four literal methods (contains/matchesWord/startsWith/endsWith) and passing it through for `regularExpression`, filtering zero-length matches, and reporting `.invalidRegularExpression` when a regex fails to compile; `nearestMatchIndex(to:in:)` binary-searches the cursor-nearest match. `FindPanelModel.swift` is the `@Observable @MainActor` view-model (adopting the Observation macro per the plan's Resolved decisions, not the old `ObservableObject`): it owns panel state (mode, query, replacement, method, match-case, wrap-around, matches, current index, inline error) and drives find, next/previous (moving the visible selection through the target and wrapping when enabled), single replace (mutating the document then landing the selection on the inserted text and re-scanning), and Replace All. Replace All runs one `beginUndoGrouping`/`endUndoGrouping` around one `textStorage` editing batch, replacing from the last match to the first so earlier offsets stay valid and the whole sweep undoes as a single operation; the final selection lands on the last replacement, its location shifted by the cumulative length change of the earlier replacements. `FindPanelView.swift` is a plain SwiftUI bar (no AppKit) hosted at the top of `CodeFileView` when presented, with a method picker, match-case toggle, find field (focused on appear, submit moves to next match), match count, prev/next, a find/replace mode toggle, the replace row, and an inline red error line for invalid regex. Wiring: `EditorCommandRouter` gained a per-window find-model registry parallel to its editor registry (keyed on the same document identity, resolved through one shared `resolvedWindowKey` helper so editor and find-model lookups always agree on the key window); `requestFind()`/`requestFindAndReplace()` keep firing their `FIND_REQUESTED mode=<find|replace>` markers and now also present the key window's panel in find or replace mode. `CodeFileView` creates one `FindPanelModel` per window, binds it to that window's editor and registers it as the text view becomes ready. Added `FindPanelTests.swift` (13 Swift Testing cases against a headless `TextView`): literal metacharacter escaping, regex matching, invalid-regex error, populated/zero matches, invalid-regex inline error, next/previous moving the selection, wrap-around, single-replace selection-on-replaced-text, Replace All non-overlapping left-to-right (`aa` in `aaaa`), Replace All last-selection with a length change, Replace All single-undo, and replace-mode presentation. Verified: `swift build` "Build complete!" zero warnings; `swift test` 116 tests in 13 suites pass with exactly 4 known issues (the WP-L1..L4 lifecycle parity contract); `pytest tests/test_ascii_compliance.py tests/test_markdown_links.py` 962 passed. Package deletion is WP-F1 patch 19, a separate patch.
- WP-F5 patch 15: built the SwiftUI Settings scene (Cmd+,) at `CodeEdit/Features/Settings/` (new folder), wired into `SwiftlyCodeEditApp.swift` in place of WP-S1's placeholder `ShellSettingsView`. `SettingsWindowView` is a three-tab `TabView` (`General`/`Theme`/`Editing`) built entirely from standard SwiftUI `Form`/`Picker`/`Stepper` controls. `FontSettingsView` reuses the pre-existing `PlainEditor.fontFamily`/`PlainEditor.fontSize` `@AppStorage` keys the command-bar ribbon's A-/A+/Reset shortcuts already write, so the ribbon and Settings never disagree. `ThemeSettingsView` persists a theme name under the new `PlainEditor.themeName` key, populating its picker options by calling `ThemeRepository.loadAllThemes()` off the main actor (`.task` + `Task.detached`) and invalidating `ThemeRepository`'s resolved-theme cache on every change so a just-edited user theme file is re-read rather than served stale. `EditingSettingsView` persists indentation style (tabs/spaces), indentation width (1-8), and the default line ending for new files (LF/CRLF, defaulting to LF per the plan's Resolved decisions) under three new keys -- these two are persisted and exposed only; actual editor consumption (auto-indent, new-document line endings) lands with the document-side packages, per the work package's explicit scope note. New `CodeEdit/Features/Settings/PlainEditorSettingsKeys.swift` centralizes every new `UserDefaults` key plus the `IndentationStyle`/`LineEndingPreference` enums and a `currentThemeName()` reader, since `PlainSyntaxHighlighter` is a plain `enum` (not a `View`) and cannot hold `@AppStorage` -- it reads the same key directly instead of receiving a theme name threaded through every `highlight(...)` call site. Live-apply: font changes propagate because every open `CodeFileView` holds the same `@AppStorage` keys, so `editorFont` recomputes and `PlainTextEditorView.updateNSViewController` applies the new `NSFont` to the live `TextView`; theme changes propagate because `CodeFileView` now also observes `PlainEditor.themeName` and re-triggers `PlainSyntaxHighlighter.highlight(...)` on change, which re-resolves the theme on its next pass (including the cache-hit fast path, so no recomputation of spans is needed). Added the `SETTINGS_APPLIED key=<name>` debug marker at each view-application site -- `PlainTextEditorView.updateNSViewController` (`key=fontFamily`, `key=fontSize`, logged only when the live `TextView`'s font attributes actually differ from the incoming value) and `PlainSyntaxHighlighter.applyHighlight` (`key=theme`, logged only when the resolved theme's name differs from the last name applied to that document's highlight state) -- never from the `@AppStorage` write itself. Added `PlainEditorSettingsKeysTests.swift` (4 Swift Testing cases: theme-name fallback and explicit-value reads through `currentThemeName()`, `IndentationStyle` display-name distinctness, `LineEndingPreference` raw-value round-trip). Documented the Settings window in `docs/USAGE.md`. The one-line smoke-script assertion for the `SETTINGS_APPLIED` marker (font size and theme) is deferred to whoever owns `scripts/plain_editor_smoke.sh` next, per this work package's launch-discipline boundary against the concurrently-running WP-S3 lane.
- WP-F4 patch 2: wired the five `PlainEditorTextCleaner` transforms added in patch 1 into a Clean Text submenu under Edit (`CodeEdit/App/Commands/EditorCommands.swift`), alongside the existing Trim Trailing Whitespace item: Normalize Line Endings to LF/CRLF, Ensure Final Newline, Convert Tabs to Spaces, Convert Spaces to Tabs, and Convert Smart Punctuation to ASCII (labeled as an explicit opt-in; nothing else applies it silently). No keyboard shortcuts, matching the shortcut-parity inventory. `EditorCommandRouter` gained matching `(on:)`/no-arg action pairs following `cleanText(on:)`'s pattern -- each is a single whole-document `replaceCharacters` call, so it undoes as one operation -- refactored through a shared private `applyCleanTransform(on:transform:)` helper. Tabs/spaces conversion uses a hardcoded `cleanTabWidth = 4`; the Settings scene (WP-F5, in flight on a separate lane) will own this as a persisted preference, not read here yet to avoid coupling to that unreviewed lane. Converted `PlainEditorTextCleaner.LineEndingStyle` to a standard `String`-backed `RawRepresentable` enum (`case lf = "\n"`, `case crlf = "\r\n"`) per patch 1 quality-review follow-up, and added the other follow-up tests: idempotence for all five transforms, a mixed leading `" \t "` indentation case for `convertSpacesToTabs`, and `tabWidth == 1` cases for both tab converters (`PlainEditorTextCleanerTests.swift`). Added `EditorCommandRouterRoutingTests.cleanLineEndingsToLFIsSingleUndoableOperation`, proving apply-then-undo-once fully restores the original CRLF text. Extended the DEBUG command self-test marker in `CodeFileView.swift` past `cleanText=true`/`cleanUndo=true`/`cleanRedo=true` with `cleanLineEndings=true cleanFinalNewline=true cleanTabsToSpaces=true cleanSpacesToTabs=true cleanSmartPunct=true`, each exercised against a small dedicated document fixture. Documented every Clean Text action in `docs/USAGE.md`.
- WP-F4 patch 1: grew `PlainEditorTextCleaner` from the single shipped trailing-whitespace trim to five additional pure text-transform functions -- `normalizeLineEndings(in:to:)` (LF/CRLF, scalar-level so a CRLF pair is treated as one line ending despite Swift merging it into a single Character), `ensureFinalNewline(in:using:)` (scalar-level trailing-ending check, empty input stays empty), `convertTabsToSpaces(in:tabWidth:)` (column-aware expansion, resets at each line boundary), `convertSpacesToTabs(in:tabWidth:)` (leading-indentation only, matching the plan's guidance not to munge interior alignment spaces -- each line's leading run of spaces/tabs is measured to a column width and re-rendered as the fewest tabs plus a spaces remainder, while text after the first non-whitespace character is untouched), and `normalizeSmartPunctuationToASCII(in:)` (curly quotes, en/em dashes, and ellipsis to ASCII, every other Unicode scalar passed through unchanged). Added 24 new Swift Testing cases with inline fixtures to `PlainEditorTextCleanerTests.swift` (86 tests total repo-wide), including a CRLF-grapheme-cluster regression test for each new function and a Greek/CJK/emoji preservation test guarding against the deleted `PlainTextCleaner`'s old codepoint-to-`?` defect. Menu wiring (WP-F4 patch 2) is deferred until the Commands menu (WP-S2) lands.
- Added the WP-V1 independent verification audit report at `docs/active_plans/audits/milestone_claims_verification.md`, covering 40 milestone claims with 26 confirmed and 14 corrected against evidence.
- Added live-engine tests for `#pop!ctx` stack transitions and step-budget truncation in `Packages/CodeEditSyntaxDefinitions/Tests/`.
- Added `PlainEditorClipboardTests` with three tests, including an external-pasteboard regression guard.
- Added four encoding lifecycle tests covering Latin-1, Windows-1252, short-high-byte fallback, and undecodable-input failure.
- Added two cleaner edge-case tests covering lone CR line endings and a final line without a trailing newline.
- Added a validation-only `--kill-after=N` CLI flag to the app.
- Made the smoke script print `SMOKE_EXIT=<code>` to stderr on every exit.
- Added a 32-way concurrent `highlightSpans` regression test proving `SyntaxDefinitionRepository`'s lock-guarded lazy loading returns identical spans under concurrency.
- WP-Q3: the app logs `LAUNCH_TO_WINDOW_MS=<n>` (measured from the start of `main()` to the first document window ordered front); new `tests/e2e/e2e_launch_time.py` launches the built app five times with `--kill-after=3`, parses the marker, reports min/median/max, and fails when the median exceeds the 1000 ms budget; baseline recorded at `test-results/perf/launch_time.txt` (median 173 ms on MacBookPro18,3); the smoke script gates on the marker line.
- WP-Q4: new `tests/e2e/e2e_screenshot_colors.py` crops a 10% border from the smoke screenshot, quantizes colors, separates grayscale from chromatic buckets, bins chromatic buckets into 30-degree hue families, and fails below 3 hue families; the smoke script runs it as a hard gate on the screenshot-captured branch (current capture: 4 hue families, the dominant three being green, blue, and orange).
- Added `scripts/make_app_icon.py`, which generates the app icon programmatically with Pillow: a lightning bolt between angle-bracket chevrons, flat two-tone (charcoal navy tile, electric yellow glyphs), rendering all macOS iconset sizes and assembling `Resources/SwiftlyCodeEdit.icns` with Pillow's native ICNS writer (no `iconutil` dependency); a 512px preview is saved at `docs/screenshots/app_icon_preview.png`, and the image-evaluator assessment (verdict SHIP after an inset/chevron rework) is recorded at `docs/active_plans/reports/app_icon_assessment.md`.
- Added `scripts/make_app_bundle.sh`, which assembles `build/SwiftlyCodeEdit.app` from the SwiftPM binary (release by default, `debug` argument accepted), writing Info.plist (CFBundleName/DisplayName SwiftlyCodeEdit, identifier org.vosslab.SwiftlyCodeEdit, icon wired, version read from `VERSION`) and validating it with `plutil -lint`; documented in `docs/DEVELOPER_USAGE.md`.
- WP-Q1: split the Kate syntax pipeline into separately callable display-free public stages on `CodeEditSyntaxDefinitions` (`parseDefinition` -> `definition(forLanguage:)` -> `tokenRuns` -> spans producing `[HighlightSpan]` with an additive `nsRange` field); the headless benchmark now prints `HIGHLIGHT_BENCH` plus per-stage `HIGHLIGHT_BENCH_STAGES` (parseMs=6 interpretMs=54 spanMapMs=2), recorded in `test-results/perf/highlight_cold_pass.txt`.
- Added WP-F0's shared `CodeEdit/Features/Support/UserDataDirectories.swift` helper defining the Application Support path policy (themes/syntax subdirectories, test-override root), with `UserDataDirectoriesTests` covering 7 tests.
- Added `docs/THEME_FORMAT.md` (WP-F2 Patch A): a versioned YAML theme schema with light/dark variants, token keys drawn from `HighlightToken` plus Kate `styleName` refinements, a fallback chain, and a required background field.
- Added `docs/HUMAN_GUIDANCE.md` recording the SwiftUI-first architecture principle (AppKit only as a last-resort escape hatch behind a replaceable adapter) and working-style guidance; linked from `AGENTS.md`; `docs/CODE_ARCHITECTURE.md` gained an "Architecture boundary" section.
- Reworked `scripts/make_app_icon.py` to write the ICNS purely with Pillow (no `iconutil`); final geometry reworked to a 10% inset with the chevron angle restored, with preview evidence saved under `docs/screenshots/`.
- Added `MILESTONE3_CHECKLIST.md`, tracking the eight SCOPE.md feature gaps (SwiftUI shell migration, Find/Replace, theme data files, user syntax directories, expanded Clean Text, large-file performance, Liquid Glass chrome, document lifecycle correctness) mapped to their `docs/active_plans/active/scope_closure_plan.md` work packages.
- Added `docs/ROADMAP.md`, listing planned work in priority order (SwiftUI shell migration, Find/Replace, theme data files, user syntax directories, Clean Text menu, large-file performance, Liquid Glass) and the SCOPE non-goals intentionally not started, pointing to `MILESTONE3_CHECKLIST.md` and `docs/active_plans/active/scope_closure_plan.md` for execution detail.
- Added `docs/NEWS.md` summarizing today's landmarks (product rename to SwiftlyCodeEdit, the 6293ms->67ms cold-highlight speedup, and the 721-file legacy purge); `docs/RELEASE_HISTORY.md` was intentionally left unwritten since zero releases/tags exist yet for this pre-release fork.
- Added `docs/INSTALL.md` and `docs/USAGE.md`, documenting the SwiftPM build/install path and the plain-editor menu commands, font controls, and status-bar fields against the current surviving source tree.
- Added `docs/FILE_FORMATS.md` documenting the text encodings `CodeFileDocument` reads/writes (UTF-8, UTF-16 BE/LE, Windows-1252, Latin-1 with explicit decode-failure handling), the bundled Kate syntax definition XML corpus (409 files), and a pointer to `docs/THEME_FORMAT.md` for the theme YAML schema.
- Added `docs/TODO.md` as the backlog scratchpad for small tasks: three live-code TODO markers (`Loopable.swift`, `Limiter.swift`, `CodeEditKeychain.swift`), the `debugRuntimeLog` single-logging-seam cleanup noted in `ms_entry_criteria_scout.md`, and the LOW Finding 7 encoding-refresh-on-reload verification item from `document_lifecycle_audit.md`.
- Added `docs/TROUBLESHOOTING.md` with 7 symptom/cause/fix entries covering smoke screenshot TCC skips, `SMOKE_EXIT` semantics, concurrent-build highlight-wait flakiness, autosave-over-live-file risk during manual testing, unsupported/BOM-less-UTF-16 encoding behavior, large-file (~1MB+) cold-highlight latency, and open-launched app instances needing a manual Dock/Cmd+Q quit.
- Added a "Proper settings dialog (WP-F5)" section to `MILESTONE3_CHECKLIST.md` (user-requested): a SwiftUI Settings scene (Cmd+,) for static preferences -- persisted font family/size, theme selection, indentation and line-ending defaults, live-apply -- with the WP-F5 candidate entry recorded in `docs/active_plans/active/scope_closure_plan.md`.
- WP-F3: `SyntaxDefinitionRepository` now merges bundled Kate XML with user-authored files discovered under `~/Library/Application Support/SwiftlyCodeEdit/Syntax/`, keyed by lowercased filename stem so a same-named user file wins a collision over its bundled counterpart; a malformed user file is logged to stderr and skipped once (retried at most once per process), leaving every other definition, bundled or user, working normally. Added `Packages/CodeEditSyntaxDefinitions/Sources/CodeEditSyntaxDefinitions/UserSyntaxDirectory.swift`, a package-local mirror of `UserDataDirectories`'s `Syntax/` path policy (the package is dependency-free and cannot import the `CodeEdit` app target's `UserDataDirectories.swift`, so the two files must be kept pointing at the same directory name if either changes) with an `overrideRoot` test seam. Added `SyntaxDefinitionLoader.mergedFileURLs(bundled:user:)` and a test-only `SyntaxDefinitionRepository(fileURLs:)` initializer so new coverage never mutates `.shared`'s process-wide cache (the one the 32-way concurrency regression test exercises). New `UserSyntaxDirectoryMergeTests.swift` (7 tests, Swift Testing) covers empty-directory discovery, filename-stem keying, collision precedence, a brand-new user language parsing to real spans (proving the "relaunch, no rebuild" behavior at the repository-API level), and the malformed-file skip-and-continue path.
- WP-F2: added `CodeEdit/Features/Theming/` (`ThemeColor`, `ThemeVariant`, `SyntaxTheme`, `ThemeSchemaKeys`, `ThemeParser`, `ThemeRepository`), a data-file theme loader that parses the `docs/THEME_FORMAT.md` schema (a schema-specific YAML-subset reader plus a `JSONSerialization` reader, since the schema is documented as format-neutral) with the documented fallback chain: a missing token or style key falls back to `base_text` at lookup time; a variant missing `base_text` or `background` is dropped and the missing-variant rule reuses the other present variant; a file that fails every check (bad syntax, unknown `version`, no usable variant) falls back to the bundled default theme with a `debugRuntimeLog` warning naming the file, never crashing or blocking startup. Bundled light and dark variants ship in one file, `CodeEdit/Features/Theming/Resources/Themes/standard.yaml`, whose hex values were measured directly from `NSColor.systemGreen`/`.systemBlue`/`.secondaryLabelColor`/etc. resolved under `NSAppearance.aqua`/`.darkAqua` on the current toolchain, so the cutover reproduces the prior hardcoded `PlainSyntaxTheme.standard` palette unchanged (including `secondaryLabelColor`'s alpha channel, carried through the schema's `#RRGGBBAA` form). User themes load from `~/Library/Application Support/SwiftlyCodeEdit/Themes/` via `UserDataDirectories` (its first real call site, alongside `WP-F3`'s), layered by schema `name` with the user copy winning a collision, per the plan's "Theme collision identity" decision. Added `ThemeParserTests` (11 tests: schema parsing, both fallback rules, malformed-file rejection, JSON acceptance) and `ThemeRepositoryTests` (8 tests: bundled discovery, `overrideRoot`-isolated user-theme layering and name collision, malformed-user-theme skip, name-resolution fallback).
- WP-S1: added the single SwiftUI `App` entry point `CodeEdit/App/SwiftlyCodeEditApp.swift` (a `Settings` scene plus a minimal File command group routing New/Open/Save/Save As/Close) and the sanctioned document-layer AppKit bridge `CodeEdit/Features/Documents/CodeFileDocument/CodeFileDocumentBridge.swift`, which holds the `ShellAppDelegate` launch path, the `NSDocumentController` glue, and the `NSWindowController`+`NSHostingController` window hosting; all new AppKit stays inside the bridge file.
- WP-S2: replaced the hand-built `PlainEditorMainMenu` NSMenu with SwiftUI `Commands`, added in `CodeEdit/App/Commands/`. `EditorCommands.swift` declares the File (New Cmd+N, Open Cmd+O, Save Cmd+S, Save As Cmd+Shift+S, Close Cmd+W), Edit (Undo Cmd+Z, Redo Cmd+Shift+Z, Cut Cmd+X, Copy Cmd+C, Paste Cmd+V, Select All Cmd+A, Clean Text no-shortcut), Find (Find Cmd+F, Find and Replace Cmd+Opt+F), and Format menus, attached to the app scene via `SwiftlyCodeEditApp.commands { EditorCommands() }`; every shortcut from the menu-shortcut inventory is preserved exactly. `EditorCommandRouter.swift` is the single shared action router -- an `@Observable @MainActor` type replacing the old `ObservableObject` `PlainEditorActionRouter` (which stays defined in the unreachable AppKit shell until WP-S4) -- and both the Commands menu and the in-window ribbon call its functions, so a menu item and its ribbon button run the same code path. Undo/redo resolve through the active `TextView.undoManager` (single undo owner; nothing sets `\.environment(\.undoManager)`). Find items are placeholders that log a `FIND_REQUESTED mode=<find|replace>` marker; the panel arrives in a later package. The Format menu is present-but-minimal (a single disabled item) since the old menu bar had no Format menu and font controls move to the Settings scene (WP-F5) and font menu (WP-F6); no shortcut parity is owed there. SwiftUI's default `.textEditing` group is emptied so Cmd+F stays single-bound to the top-level Find menu. Document commands (New/Open/Save/Save As/Close) route through the sanctioned document bridge `ShellDocumentActions`, and the ribbon's New/Open/Save/Save As buttons were repointed from direct `NSDocumentController`/`NSApp.sendAction` calls to the same bridge helpers. Verified: `./build_debug.sh` "Build complete!" zero new warnings, `swift test` 86 tests pass with exactly 4 known issues (the WP-L1..L4 lifecycle parity contract), `./scripts/plain_editor_smoke.sh` `SMOKE_EXIT=0` with the command self-test line all-true (`insert=true undo=true redo=true selectAll=true copy=true cut=true paste=true cleanText=true cleanUndo=true cleanRedo=true`) and the `Main menu items:` marker listing File, Edit, Find, and Format.
- WP-Q5: added a keystroke latency E2E harness. `CodeEdit/Features/Editor/Views/PlainEditorKeystrokeBench.swift` is a DEBUG-only, `CODEEDIT_KEYSTROKE_BENCH=<n>` env-gated bench wired next to the existing command self-test in `CodeFileView.swift`'s `onTextViewReady`; it drives `n` single-character insertions through `TextView.replaceCharacters` at offsets cycled across the document (the same synchronous path a real keystroke takes, confirmed by reading `TextView+ReplaceCharacters.swift`'s `NotificationCenter.post`), scheduling one edit per run-loop turn via `DispatchQueue.main.async` recursion (not a blocking for-loop, which would starve the run loop for the whole multi-minute run and its own `--kill-after` backstop timer), and logs one `KEYSTROKE_MS=<float>` line per edit plus `KEYSTROKE_BENCH_DONE=<n>` through `debugRuntimeLog`. New `tests/e2e/e2e_keystroke_latency.py` generates a runtime-only ~1 MB Swift-like fixture, launches the app with the bench env vars, refuses to start if another `SwiftlyCodeEdit` process is already running (`refuse_if_another_codeedit_is_running`, documenting the single-writer rule on the shared `/tmp/codeedit_runtime.log`), polls the runtime log for the done marker and terminates the process itself (the app's own `--kill-after` quit path blocks forever on the standard unsaved-changes save prompt once the bench has dirtied the document, so the harness cannot rely on the app exiting on its own), and reports min/median/p95 with a two-part gate design not both wired up yet: `--record-baseline` (default, always exits 0) records the baseline, `--gate` (reserved for WP-Q6) checks a 16 ms p95 budget plus a 20% regression threshold. The first baseline recorded at `test-results/perf/keystroke_latency.txt` (min 2377.07 ms, median 2384.14 ms, p95 2416.02 ms on a MacBookPro18,3, macOS 26.5.2, Swift 6.3.3) was later found invalid: it timed only the synchronous mutation window and stopped the clock before the async span compute and main-thread attribute paint a keystroke actually triggers, so it could never show WP-Q6's future improvement (corrected under Fixes and Maintenance). The valid end-to-end baseline is min 2627.47 ms, median 10662.09 ms, p95 15233.04 ms (git 028868f, same hardware/toolchain) -- the whole-document re-highlight-per-keystroke cost dominates at this fixture size, far past the eventual 16 ms budget, exactly the gap M8 exists to close.
- WP-Q8 wired the M8 keystroke ship gate to `KEYSTROKE_MUTATION_MS` (the synchronous first-paint
  slice), not `KEYSTROKE_MS` (full highlight settle), per the M8 keystroke gate decision
  (`docs/active_plans/decisions/m8_keystroke_gate_decision.md`).
  `tests/e2e/e2e_keystroke_latency.py --gate` now checks mutation p95 against the 16 ms absolute
  budget plus a 20% regression threshold over the recorded mutation baseline; `--record-baseline`
  records both the mutation (ship-gate) series and the settle (background-freshness) series,
  clearly labeled, to `test-results/perf/keystroke_latency.txt`. On the 1 MB / 44,884-line fixture
  (git 93312b6, MacBookPro18,3): mutation p95 1.87 ms (PASS, roughly 8.6x under budget), settle
  p95 204.26 ms (tracked, not gated). This is the M8 milestone exit evidence.

### Behavior or Interface Changes

- WP-S3: made the smoke script authoritative for the SwiftUI shell. `scripts/plain_editor_smoke.sh` now hard-gates on the `SHELL=SwiftUI` marker (previously only asserted implicitly by other markers succeeding, never checked directly), and the `Main menu items:` line is no longer a bare prefix-substring check -- new `extract_menu_section`/`check_menu_has_items` helpers parse each top-level menu's own bracketed item list (anchored on a preceding `| ` so `Edit:` can never match inside `SwiftlyCodeEdit: [...]`) and assert every first-party item is present per menu (File: New, Open..., Save, Save As..., Close; Edit: Undo, Redo, Cut, Copy, Paste, Select All, Clean Text; Find: Find..., Find and Replace...; Format: Font and Text Options), while tolerating macOS's own OS-version-dependent menu injections (Writing Tools, AutoFill, Start Dictation x2, Emoji & Symbols x3, blank separators) so only a missing first-party item fails the run. `docs/SMOKE_TEST.md` hard-gate list updated to match. Verified: `./scripts/plain_editor_smoke.sh` `SMOKE_EXIT=0` (screenshot step `SKIPPED: ... likely denied screen-recording permission` in this environment, as documented), `swift test` 98 tests in 11 suites passed with exactly 4 known issues (the WP-L1..L4 lifecycle parity contract: `undoToSavedContentClearsDirtyFlag`, `externalChangeWithUnsavedEditsSurfacesConflict`, `undecodableExternalChangeSurfacesError`, `externalReloadInvalidatesStaleUndoOperations`), `pytest tests/test_ascii_compliance.py tests/test_markdown_links.py` 962 passed.
- WP-S1: flipped the app entry point to SwiftUI in one change -- removed `@main` from the AppKit `CodeEditMain` enum (now unreachable dead code kept until WP-S4) and made `SwiftlyCodeEditApp` the sole `@main`. Document windows are hosted through `NSDocumentController` under a plain SwiftUI `App` scene (not `DocumentGroup`) per `docs/active_plans/decisions/document_architecture_decision.md`, with `CodeFileDocument.makeWindowControllers()` delegating window construction into `CodeFileDocumentBridge`. The runtime log gains a `SHELL=SwiftUI` marker; New/Open/Save/Save As/Close, the 2-second autosave debounce, and external-change reload into the shared `NSTextStorage` all still work. Verified: `./build_debug.sh` "Build complete!" (zero new warnings), `swift test` 63 tests pass with exactly 4 known issues (the WP-L1..L4 lifecycle parity contract), `./scripts/plain_editor_smoke.sh` `SMOKE_EXIT=0`, and `tests/e2e/e2e_launch_time.py` median 251 ms (< 1000 ms budget).
- Changed paste to always read `NSPasteboard.general` directly; removed the private `copiedText` buffer so pasting external-app content is no longer overridden by a stale internal value.
- Changed non-UTF file opening to decode via Windows-1252 or Latin-1 fallback, or present a real error alert, instead of silently opening a blank document.
- Changed the status bar to report "Unknown" instead of falsely claiming "UTF-8" when no decoding was actually applied.
- Reclassified smoke screenshot capture as an optional diagnostic step with explicit `SKIPPED: <reason>` lines and a `--no-screenshot` flag.
- Changed `build_debug.sh` and the smoke script to launch the app with `--kill-after` so validation runs never leave stray instances in the Dock.
- Removed the font-family dropdown from the editor command ribbon; the persisted `PlainEditor.fontFamily` setting and the font size controls remain.
- Renamed the product to SwiftlyCodeEdit: the executable product name in Package.swift, the app menu title and Quit item, and launch/kill tooling (`build_debug.sh`, `build_release.sh`, the smoke script, `tests/e2e/e2e_launch_time.py`); the target name and `CodeEdit/` source directory are deliberately kept as-is to avoid cascading `@testable import` breakage, with the directory rename owed to the purge work package.
- Changed the keystroke-latency baseline file schema: flat `p95_ms`/`min_ms`/`median_ms` fields
  are replaced by explicitly labeled `mutation_*` (ship gate) and `settle_*`
  (background-freshness) fields in `test-results/perf/keystroke_latency.txt`.

### Fixes and Maintenance

- WP-Q2 quality-review fix: the cursor-label dedup in `PlainEditorChromeModel.updateCheapMetrics`
  (`CodeEdit/Features/Editor/Views/CodeFileView.swift`) keyed its skip signature only on
  `(location, length, documentLength, cachedTotalLines)`, so an equal-length text edit entirely
  before the cursor -- one that leaves the raw cursor offset, document length, and total line
  count all numerically unchanged while moving where a newline sits inside that prefix -- produced
  a bit-identical signature and kept a stale, wrong column at rest (reachable through Find/Replace's
  shipped equal-length regex substitution). Fixed by adding an `editGeneration` counter, bumped in
  `refresh` and `refreshForEdit` (every path that corresponds to a `.range`/`.fullInvalidation`
  document change) and folded into `CursorSignature`, so an edit always forces a recompute while
  `refreshForSelectionChange` (a pure selection move with no edit) never bumps it, so a genuine
  cursor bounce with no intervening edit still collapses to a no-op. Added
  `cursorLabelRecomputesAfterEqualLengthEditBeforeCursor` to `PlainEditorStatusReporterTests.swift`,
  reproducing the exact "abc\ndefghi" -> "xy\nzdefghi" prefix substitution (confirmed to fail
  against the pre-fix dedup and pass after), plus two direct `cursorLabel` edge-case tests (empty
  document, and a document with no trailing newline). Verified: `swift build` "Build complete!"
  zero new warnings; `swift test` 144 tests in 17 suites pass.
- WP-Q2: eliminated the O(n)-per-keystroke status-bar floor. After WP-Q6 bounded the highlighter, the plain editor still spent a flat ~2 s per keystroke on the status bar: `PlainEditorChromeModel.refresh` ran ~3x per keystroke (onTextChange once plus onSelectionChange twice from cursor bounce), each call bridging the whole `NSTextStorage` to a fresh Swift `String` and running ~8 O(n) passes over the 1 MB buffer -- two `components(separatedBy:.newlines)` splits in the cursor label, more full splits for line count and indentation, a Unicode-grapheme `wordCount` scan, and up to two `contains` scans for the line ending (measured shipped baseline p95 2057.97 ms, median 2023.05 ms per keystroke; `test-results/perf/keystroke_latency_candidates.txt`). Fixed with a hybrid design that separates the immediate cursor from the expensive counts. `PlainEditorStatusReporter` (`CodeEdit/Features/Editor/PlainEditorStatusReporter.swift`) was rewritten to operate on the live `NSString` backing store (`NSTextStorage.mutableString`, no per-keystroke String copy) with bounded, chunked UTF-16 scans and an ASCII word-character fast path, so no function allocates a per-line or per-word array; line counting treats a CRLF pair as one break (a Windows-lineending file is no longer double counted) and stays consistent between the cursor numerator and the total-line denominator. `PlainEditorChromeModel` (`CodeEdit/Features/Editor/Views/CodeFileView.swift`) now has three entry points: `refreshForEdit` (onTextChange) and `refreshForSelectionChange` (onSelectionChange) update only the cheap metrics -- the character count from the storage length (O(1)) and the cursor label against a cached total-line count -- while the O(n) counts (word, line, indentation, line ending) run on a 150 ms debounced, cancellable main-actor `Task`; `refresh` stays the synchronous full path for initial load, external reload, and onAppear so the status bar is correct the moment a document opens. The cursor scan is coalesced by a selection/length/total signature, so the two same-selection refreshes of a keystroke's cursor bounce collapse to no-ops. Counts stay correct after paste, undo, and Clean Text because the debounced recompute is a full recount of the current buffer regardless of how it got there. New `PlainEditorStatusReporterTests.swift` pins the fast scans against a brute-force word-count oracle and explicit line/cursor/indentation/line-ending expectations across typing, paste, undo, Clean Text, CRLF, and chunk-boundary inputs, plus a chrome-model wiring case. Measured result on the M8 keystroke gate (`tests/e2e/e2e_keystroke_latency.py`, 200 edits over a 1,000,367-char fixture, MacBookPro18,3): p95 fell from 2057.97 ms to 223.15 ms (median 2023.05 -> 193.10 ms), an 89% cut that eliminates the status floor. Attribution from the STATUS_REFRESH_MS sub-measurement: the synchronous status cost on the hot path is now ~12 ms (the bounded cursor-numerator scan, which is O(cursor offset) deep in the fixture and is all that remains of status on the keystroke path), against a ~166 ms end-to-end KEYSTROKE_MS whose ~150 ms residual is TextKit mutation and relayout at 44,884 lines, not the status bar or the highlighter. Verified: `swift build` "Build complete!" zero new warnings; `swift test` 141 tests in 17 suites pass; `pytest tests/` 2011 passed; `./scripts/plain_editor_smoke.sh --no-screenshot` `SMOKE_EXIT=0` with the `Plain editor status: cursor=` marker still firing.
- WP-Q6 bounded-rehighlight regression fix: `PlainTextEditorView.updateNSViewController` (`CodeEdit/Features/Editor/Views/PlainTextEditorView.swift:148-163`) called `onTextStorageReady?(textStorage)` unconditionally on every SwiftUI update. SwiftUI re-runs `updateNSViewController` on each keystroke (a `chrome.refresh` `@Published` mutation re-renders `CodeFileView`), so this scheduled a whole-document highlight (223,305 spans over the 1 MB fixture, ~1997 ms) on every edit; that full pass bumped the shared per-storage generation and superseded the bounded WP-Q6 rehighlight, which never ran its region compute (the `WPQ6_BOUNDED` marker was absent for all 200 keystroke-bench edits). The intended guard (`activeTextView == nil`, `CodeFileView.swift:129`) could not hold because a `@State` value read inside an escaping closure invoked from `updateNSViewController` does not reflect the live post-mount value. Fixed by firing `onTextStorageReady` only when the backing store actually changed (`storageDidChange = textView.string != textStorage.string`); a same-storage keystroke no longer reaches it. The initial highlight still runs from `makeNSViewController` (plus `onTextViewReady`), and an external reload re-highlights through the document read path (`CodeFileDocument.read(from:ofType:)` calls `PlainSyntaxHighlighter.highlight` in place), so no highlight is lost. After the fix and `./build_debug.sh` "Build complete!" (zero new warnings), the keystroke bench log shows `WPQ6_BOUNDED` firing once per edit (200x), whole-document `PlainSyntaxHighlighter start` passes dropped from 217 to 2 (startup only), and bounded regions are small (~500-1774 chars); `./scripts/plain_editor_smoke.sh --no-screenshot` `SMOKE_EXIT=0`. This removes the redundant per-keystroke full pass; the residual ~2 s floor is the separate status-bar cost recorded under Decisions and Failures.
- SwiftLint mechanical sweep (one of four parallel lanes against the `swiftlint_baseline_2026-07-10.md`
  baseline; this lane owned `CodeFileView.swift`, `CodeFileDocument.swift`, `CodeFileDocumentBridge.swift`,
  `PlainTextEditorView.swift`, `ThemeRepository.swift`, `ThemeParser.swift`, `ThemeSettingsView.swift`,
  `FontSettingsView.swift`, `SettingsWindowView.swift`, `EditorCommands.swift`, `SwiftlyCodeEditApp.swift`,
  `DebugRuntimeLog.swift`, and `PDFFileView.swift`). Cleared both serious `line_length` errors in
  `CodeFileView.swift` (the DEBUG command-self-test marker at line 464 and the DEBUG status marker at
  line 514) by rebuilding each long interpolated literal as `+=`-concatenated segments per the repo's
  string-building convention, verified byte-identical to the original emitted text via a scratch
  comparison script so `scripts/plain_editor_smoke.sh`'s marker greps are unaffected; applied the same
  concatenation fix to a `line_length` warning on the "Loaded file" DEBUG marker in
  `CodeFileDocument.swift`, moving the string-building helper into the file's existing `private
  extension` block (outside the class body) so the fix does not push `CodeFileDocument`'s
  `type_body_length` over its error threshold. Fixed roughly 25 `attributes` violations across
  `EditorCommands.swift`, `FontSettingsView.swift`, `ThemeSettingsView.swift`, `SwiftlyCodeEditApp.swift`,
  and `CodeFileView.swift` by moving `@AppStorage`/`@NSApplicationDelegateAdaptor` attributes with
  arguments onto their own line above the property, and in `PlainTextEditorView.swift` by moving `@objc`
  onto its own line above each Coordinator method. Fixed `line_length` warnings in `ThemeRepository.swift`
  (two long function signatures wrapped) and `ThemeParser.swift` (two long `guard` chains split); fixed
  `ThemeParser.swift`'s `control_statement` violation (dropped redundant parentheses around an `if`
  condition); fixed three `modifier_order` violations in `CodeFileDocument.swift` (`nonisolated` moved
  before `private`) and its `trailing_newline` violation (file was missing its final newline entirely);
  fixed `PDFFileView.swift`'s `function_name_whitespace` violation (removed the space before the function
  parameter list); fixed `DebugRuntimeLog.swift`'s `non_optional_string_data_conversion` violation
  (`Data("\(message)\n".utf8)` in place of the optional `.data(using: .utf8)` form). Left untouched, per
  this lane's scope: `file_length`/`type_body_length`/`function_body_length`/`cyclomatic_complexity` in
  these files (structural, other packages own them) and `PlainTextEditorView.swift`'s
  `notification_center_detachment` violation (a behavioral judgment call, filed as a `docs/TODO.md` entry
  instead of a mechanical fix, since the observer is deliberately removed outside `deinit` to support
  re-attaching to a new `TextView`). Verified: `swift build` "Build complete!" zero new warnings;
  `swift test` 134 tests in 16 suites passed with zero known issues; `swiftlint lint --quiet CodeEdit`
  dropped from 59 to 34 violations repo-wide (both serious errors cleared, zero errors remaining, the
  mechanical bucket cleared from this lane's owned files); `pytest tests/` 2011 passed.
- WP-L4: closed document-lifecycle audit finding F4 (a reload left the editor's undo stack stale). An external-change reload mutates the shared `NSTextStorage` in place (`content.mutableString.setString(...)`) to preserve object identity, which deliberately bypasses `TextView.setTextStorage`'s built-in `clearStack()`, so undo operations registered before the reload kept referencing pre-reload offsets and substrings -- a post-reload Undo could replay a stale operation against mismatched content, corrupting the reloaded text or raising an out-of-range `NSException`. Fixed at the ownership boundary rather than in the document: the document only announces that the whole buffer changed (it already broadcasts `EditedTextChange.fullInvalidation` from its reload path), and the editor layer that owns the undo manager responds. `CodeFileView`'s existing reload observer (registered in `onTextViewReady`, keyed by `didObserveReloads`) now also resets the active text view's `CEUndoManager` via `textView._undoManager?.clearStack()` on a `.fullInvalidation`, so a post-reload Undo is a clean no-op (empty stack) instead of a corrupting replay. The reset lives in `CodeFileView`, not in the document or the `PlainTextEditorView` adapter, so document logic stays out of the undo seam and the generic adapter stays free of `CodeFileDocument` awareness -- `docs/CODE_ARCHITECTURE.md` gains an "Undo manager ownership" section recording that the text view's `CEUndoManager` is the single undo owner after the migration. The F4 expected-fail test `externalReloadInvalidatesStaleUndoOperations` now wires the same reset observer the production editor uses and asserts the fixed behavior directly (`undoManager.canUndo == false` and the reloaded text intact after Undo), with its `withKnownIssue` marker removed -- after which no expected-fail marker exists anywhere in the suite (the M3 exit rule). Verified: `swift build` "Build complete!" zero new warnings; `swift test` 133 tests in 16 suites pass with zero known issues; `pytest tests/` 1998 passed.
- WP-L3: closed document-lifecycle audit finding F3 (a reload silently swallowed decode failures via `try?`) and audit finding F7 (encoding label refresh on reload). The clean-document branch of `CodeFileDocument.handleExternalContentChange` advanced `fileModificationDate` to the new mtime and then called `try? self.read(from: fileURL, ofType: fileType)` unconditionally, so an external tool rewriting a clean file in an unsupported encoding advanced the date behind stale in-memory text with no alert -- silently violating the "always real text or an explicit error, never a silent blank" contract (WP-V3) that previously covered only the initial open path. Killing the `try?` swallow: the clean branch now classifies the external bytes first (`Data(contentsOf:)` + `decode(data:)`) without touching the buffer, surfaces the `.undecodable` error alert (via the `pendingExternalChange` surface WP-L2 built) with the buffer untouched and the date unmoved when the bytes do not decode, and only reloads and advances the date when the reload actually succeeds. This wires the clean + undecodable row of the five-case external-change matrix, extending the never-silent-blank contract to reload as well as initial open. F7 (encoding status label refresh on successful reload) is already closed by WP-L2's `CodeFileView` reload observer, which calls `chrome.refresh` on `.fullInvalidation` (the encoding label reads `document.sourceEncoding`, re-detected by the reload's `read` path); this patch cites that observer rather than re-implementing it. The same silent-swallow pattern was also fixed on the conflict-resolution path (found in WP-L2 review): `resolveExternalChangeConflict(reloadFromDisk:)` called `try? self.read(...)` and then `updateChangeCount(.changeCleared)` unconditionally, so a reload that failed (the file changed again between prompt and click, or its new bytes no longer decode) marked the document clean while the user's kept edits still sat in the buffer -- a silent data-loss bug, since the next window close would offer no save. It now branches on the throw: a successful reload clears the dirty flag as before, and a failed reload keeps `isDocumentEdited` true and surfaces the `.undecodable` error instead. The F3 expected-fail test `undecodableExternalChangeSurfacesError` now asserts the fixed behavior directly (buffer untouched, `fileModificationDate` unchanged) with its `withKnownIssue` marker removed; a new reload-direction encoding case `reloadRedetectsEncodingForDecodableExternalChange` in `CodeFileDocumentLifecycleTests` proves a UTF-8 file rewritten as Latin-1 reloads to the new text with the re-detected encoding; and a new `reloadChoiceWithFailedReadKeepsEditsAndSurfacesError` regression pins the conflict-resolution data-loss fix (kept edits and dirty flag preserved, error surfaced). Verified: `swift build` "Build complete!" zero new warnings; `swift test` the two lifecycle suites pass with only WP-L4's known issue remaining at this patch; `pytest tests/` green.
- WP-L2: closed document-lifecycle audit finding F2 (an external change to a dirty document was silently dropped). `CodeFileDocument.presentedItemDidChange` previously fell straight through its `guard isDocumentEdited else` when the document had unsaved edits, so the conflict was never surfaced, `fileModificationDate` stayed stale, and the next autosave was a lost update. It now routes the external-change matrix explicitly: a clean + decodable change reloads silently (encoding label refreshed); a dirty + decodable change acknowledges the observed on-disk version (advancing `fileModificationDate` so the same change is not re-prompted) and surfaces a keep-mine-or-reload conflict with the buffer left untouched until the user chooses; a dirty + undecodable change shows an error alert with edits kept and no date advance; and a deleted or moved file (detected via `probeModificationDate`'s new missing/unreadable/date distinction and an `accommodatePresentedItemDeletion` override) alerts, clears the backing `fileURL` so the next Save routes through Save As, and keeps the buffer. The `getModificationDate()` helper was replaced by `probeModificationDate()` returning a three-case `FileModificationProbe`, so a stat failure (`unreadable`) no longer masquerades as "no change" the way the old `nil`-for-everything return did. The conflict prompt is SwiftUI-side: a new observable `pendingExternalChange` on the document drives a `.alert` on `CodeFileView` (this window's scene) with no AppKit `NSAlert`; `resolveExternalChangeConflict(reloadFromDisk:)`/`dismissExternalChangeAlert()` clear it, and a reload resolution clears the dirty flag since the buffer again matches disk. The clean silent reload broadcasts `.fullInvalidation` (already wired) and now logs a DEBUG `RELOAD_COMPLETE` marker after the storage swap; `CodeFileView` also registers a weak-captured `addEditObserver` so a silent reload refreshes the status bar's encoding label. The F2 expected-fail test `externalChangeWithUnsavedEditsSurfacesConflict` now asserts the fixed behavior directly (dirty buffer kept, `pendingExternalChange == .reloadConflict`, `fileModificationDate` advanced to the on-disk mtime) with its `withKnownIssue` marker removed. New DEBUG-only seams: `PlainEditorConflictAutoChoice` reads a volatile `-PlainEditor.conflictAutoChoice keep|reload` NSArgumentDomain launch argument so an unattended e2e can answer the prompt, and `PlainEditorConflictScenarioSelfTest` (env `CODEEDIT_CONFLICT_SCENARIO=clean|dirty`, scheduled from `CodeFileView.onTextViewReady`) stages the dirty state and logs a `CONFLICT_SCENARIO_READY` readiness marker. New `tests/e2e/e2e_external_change_conflict.py` launches the app on a sandboxed `/tmp` fixture, rewrites it from the harness process (a genuine NSFilePresenter external change), and gates the clean-silent-reload and dirty-conflict-auto-resolve rows on runtime-log markers. The clean + undecodable date-advance and the post-reload stale-undo reset remain WP-L3/WP-L4's work, still pinned by their known-issue tests. Verified: `swift build` "Build complete!" zero new warnings; `swift test` 129 tests in 15 suites pass with exactly 2 known lifecycle issues remaining (WP-L3/L4); F2 passes without a marker.
- WP-L1: closed document-lifecycle audit finding F1 (undo never cleared the dirty flag). The document, not the view, now owns change tracking: `CodeFileDocument` gained `recordEdit(_:replacedRange:newLength:)`, which maps a forward edit to `updateChangeCount(.changeDone)`, an undo to `.changeUndone`, and a redo to `.changeRedone`, so undoing back to the last-saved content leaves `isDocumentEdited == false` and redoing away sets it again (and autosave stops rewriting an unmodified file, since `hasUnautosavedChanges` now clears with the flag). `CodeFileView` no longer calls `updateChangeCount`; its editor callback reports each mutation with how it reached the buffer, reading `TextView.undoManager.isUndoing`/`isRedoing`. Undo/redo replay one mutation per replaced range, so the per-range report stays balanced with the forward edits that dirtied the document. The Save button now derives from real dirty state (`canSave: codeFile.isDocumentEdited`), closing the old `... || fileURL != nil` shortcut. Explicit deliverable for M8: the document exposes a two-case `Sendable` edited-range change notification, `EditedTextChange` -- `.range(replacedRange:newLength:)` for typing, paste, undo, redo, and find-replace, and `.fullInvalidation` for external reload and Clean Text -- broadcast to observers registered via `addEditObserver`; a new per-mutation `onEdit` callback on the `PlainTextEditorView` adapter carries the replaced range and new length that feed it. The F1 expected-fail test `undoToSavedContentClearsDirtyFlag` now asserts the fixed behavior directly (edit dirties, undo-to-saved clears, redo re-dirties) with its `withKnownIssue` marker removed; two new tests (`recordEditBroadcastsBoundedRangeChange`, `externalReloadBroadcastsFullInvalidation`) pin the notification payload shape. Verified: `./build_debug.sh` "Build complete!" zero new warnings; `swift test` 126 tests in 14 suites pass with exactly 3 known issues remaining (the WP-L2/L3/L4 lifecycle parity contract); `pytest tests/` 1968 passed. The dirty-state status-bar field (audit F5, MEDIUM) is more than one line and is filed in `docs/TODO.md`.
- WP-F1 patch 20: fixed a stale-match crash in the find panel. `FindPanelModel` populated `matches: [NSRange]` only from `performFind()` (the find-field `onChange` and its own replace paths) and nothing invalidated those ranges when the document changed outside the find flow, so the sequence Cmd-F, search, click into the document, delete text before or around a match, Replace fed an out-of-bounds `NSRange` to `TextView.replaceCharacters` -> `NSMutableAttributedString.replaceCharacters` and raised `NSInvalidArgumentException`; next/previous could likewise land the selection past end-of-file through `TextSelectionManager.setSelectedRange` (which, unlike `setSelectedRanges`, has no bounds check). Fixed in the model, not with scattered guards: `CodeFileView`'s single `onTextChange` observation point now calls a new `FindPanelModel.handleExternalTextChange()`, which re-runs `performFind()` on any external edit while the bar is presented (chosen over clearing so the match count and highlights stay live and correct as the user edits; per-keystroke re-scan cost on large files is tracked as a debounce TODO for M8). The model tells its own Replace edits apart from external ones with an `isPerformingInternalEdit` flag set around every `replaceCharacters` call (via a `withInternalEdit` helper), so an internal edit never triggers a redundant re-scan. As belt-and-braces defense in depth (the WP-L4 defect class), `replaceCurrentMatch`, `replaceAllMatches`, and `selectCurrentMatch` now validate each stored range's upper bound against `textStorage.length` before acting and drop the stale matches (re-scanning) rather than touching an out-of-bounds range. Added three `FindPanelTests` cases driving the exact scenario -- populate matches, mutate the `TextView` directly to shrink the document, then confirm Replace does not crash or corrupt, next/previous does not select out of bounds, and an external edit re-scans the match list to reflect the new content. Verified: `swift test` 119 tests in 13 suites pass with exactly 4 known issues (the WP-L1..L4 lifecycle parity contract); `pytest tests/test_ascii_compliance.py` 896 passed.
- WP-Q5 measurement-validity fix: the keystroke bench's `KEYSTROKE_MS` timed only the synchronous mutation window (`replaceCharacters` plus the status refresh), stopping the clock before the highlight pass that edit triggers -- span compute AND `applyHighlight`'s main-thread attribute paint and `layoutLines` -- which the highlighter runs on later main-actor turns, so the old 2416 ms p95 baseline could never reflect WP-Q6's future bounded-rehighlight improvement. Added a DEBUG-only completion seam to `PlainSyntaxHighlighter` (`onHighlightSettled(storage:perform:)` plus an internal `markGenerationSettled`) keyed on the existing per-`NSTextStorage` generation counter; it fires after paint+layout on every terminal path (result applied, coalesce-drop, post-compute superseded-drop, drift-cap), so a superseded generation still settles and a waiter never deadlocks (each bench edit waits for its own generation or a newer one). The scheduling/coalescing behavior itself is untouched (WP-Q6 owns that); only the seam was added, staying Swift 6 strict-concurrency clean with no new `@unchecked Sendable`. The bench now drains the initial cold highlight before timing, then records each `KEYSTROKE_MS` end-to-end (mutation + status refresh + span compute + paint), still scheduling the next edit one-per-run-loop-turn via `DispatchQueue.main.async`. Also fixed the offset cycling: `(editIndex * 97) % documentLength` kept all 200 edits inside the first ~2% of a 1 MB fixture; edits now advance by a full stride (`documentLength / editCount`) plus a small prime offset so they span the whole document. The Python harness now records `git_commit=<HEAD sha>` and a `code_state` note (measurement window) in the results file, wraps the launched app in `try/finally` so it is always terminated, and polls `process.poll()` each pass so an app crash fails fast with the log tail instead of hanging to the deadline (deadlines raised to a 2400 s poll and a 2700 s `--kill-after` app backstop to fit the now-multi-minute end-to-end run). Verified: `./build_debug.sh` "Build complete!" zero warnings; `swift test` 86 tests pass with exactly 4 known issues (before a concurrent lane's WP-S2 routing tests); `pyflakes tests/e2e/e2e_keystroke_latency.py` clean; the corrected baseline run itself (200/200 edits, min 2627.47 ms, median 10662.09 ms, p95 15233.04 ms).
- WP-S2 multi-window fix: corrected `EditorCommandRouter`'s command targeting so menu and ribbon commands always act on the intended editor when more than one document window is open. The router previously held a single last-write-wins weak `activeTextView` set once per window at creation (`CodeFileView.onTextViewReady`); with two documents open, `shared.activeTextView` pointed at the last-opened window regardless of focus, so undo/redo/copy/cut/paste/selectAll/cleanText from the menu or ribbon silently hit the wrong document, and closing the last-registered window nil'd the weak slot and turned every command into a no-op for the remaining windows. Replaced the single slot with a per-window-keyed registry (`[ObjectIdentifier: WeakEditor]` keyed on the hosting `CodeFileDocument` identity) plus a key-window lookup: menu commands resolve to the key window's editor (falling back to the sole editor when only one window is open, so the launch and DEBUG self-test single-window path stays correct before any key-window event arrives), while the in-window ribbon now passes its own window's editor directly through new `undo(on:)`/`redo(on:)`/`cut(on:)`/`copy(on:)`/`paste(on:)`/`selectAll(on:)`/`cleanText(on:)` target forms -- the menu forms resolve the target and call the same functions, keeping "ribbon and menu invoke the same action functions." Key-window and close observation lives in the sanctioned document bridge (`CodeFileDocumentBridge.swift`'s new `EditorWindowKeyObserver`, which watches each window's `didBecomeKey`/`willClose` and informs the router), so `EditorCommandRouter.swift` still imports no AppKit shell symbols. Added `EditorCommandRouterRoutingTests.swift` (3 Swift Testing cases: single-registrant resolution without an active key, key-window routing selection across two registrants with refocus, and close-drops-entry rerouting). Verified: `swift build` "Build complete!" zero warnings, `swift test` 89 tests pass with exactly 4 known issues (the WP-L1..L4 lifecycle parity contract), `pytest tests/test_ascii_compliance.py` 896 passed.
- Fixed app-icon over-padding in `scripts/make_app_icon.py`: bracket vertices and arm tips (including rounded stroke caps) and the bolt tips now land exactly on the 10% content-inset lines, so the glyph spans ~80% of tile width/height instead of ~62%; bolt keeps its 0.34/0.62 width-to-height ratio and clears the right bracket by ~175 px at master size.
- Fixed a double `finishPlainEditorLaunch()` invocation on launch.
- Fixed a CRLF grapheme-cluster bug in `PlainEditorTextCleaner` where CRLF-terminated lines were never trimmed because Swift merges `\r\n` into a single `Character`.
- Deleted the destructive `PlainTextCleaner`, which mapped codepoints above U+00FF to `?`.
- Deleted the dead `PlainEditorSyntaxStyler` and its wrong-engine `KateXMLSyntaxHighlighter` test suite.
- Retargeted cleaner tests to the live `PlainEditorTextCleaner` and renamed the test file to match.
- Fixed the `.gitignore` bare `PackageSmoke` pattern that had kept the entire `CodeEditTests/PackageSmoke` suite untracked.
- Moved the first syntax highlight off the main thread: span computation now runs in a detached background task with attribute application back on the main actor, using generation-based coalescing and stale-result guards, so the document window appears immediately instead of beachballing roughly 3-6 seconds behind a synchronous cold highlight during window construction.
- WP-Q0b: fixed intermittent syntax highlighting by replacing the highlighter's global generation counter and span cache with per-document state keyed per `NSTextStorage` in a weak-key `NSMapTable`, so one window's request can no longer strand another window's in-flight compute; external-change reloads now schedule a highlight after `setString` (they bypass the text-change notification); a computed result that finds the text drifted with no newer request recomputes and applies instead of leaving the document unhighlighted; covered by a new two-document concurrency test (suite now 28 tests in 6 suites).
- Fixed a release-configuration build failure: `CodeFileView.swift` called the DEBUG-only `PlainEditorCommandSelfTest` outside any `#if DEBUG` guard, compiling in debug but breaking `swift build -c release`; the call site is now guarded.
- Removed a pasted Formula 1 standings table accidentally saved into `CodeFileDocument.swift`: a live editor session had the real source file open during manual paste testing, and the 2-second autosave wrote it to disk (also stripping the trailing newline); the appended text broke the release build and leaked into the smoke screenshot.
- Declared `numpy` in a new root `pip_requirements.txt` (alongside `pillow`) for `tests/e2e/e2e_screenshot_colors.py`; added inline bandit B108 waivers in `tests/e2e/e2e_launch_time.py` for the fixed `/tmp/codeedit_runtime.log` path contract written by `DebugRuntimeLog.swift`.
- Clarified `tests/e2e/e2e_screenshot_colors.py` output: it now prints `chromatic_buckets_total` and `chromatic_buckets_printed` separately so a single dominant printed bucket can no longer look contradictory next to the hue-family count.
- WP-Q1: fixed the cold-highlight regression, 6293 ms down to 67 ms on the ~1400-line smoke fixture; the root cause was an O(n^2) step-budget guard recomputing `text.count` per interpreter step, not regex compilation. Added a `FirstCharFilter` ASCII-bitmap prefilter (skips roughly 83% of regex attempts, with a conservative analyzer bailing to always-run), a UTF-16 `NSString` backing with lockstep offset tracking, and a process-wide `CompiledRegexCache` (warm reopen 0 ms).
- WP-Q1 review fixes: POSIX bracket expressions (`[[:cntrl:]]` and similar) are no longer misparsed by the filter analyzer, which now bails to always-run (a regression test is proven to fail without the fix); the match-jump path now advances the grapheme cursor and UTF-16 offset from one walk, eliminating a mid-cluster desync risk (covered by a unicode fixture test spanning emoji, CJK, and combining marks).
- Fixed silent corruption of BOM-less UTF-16 files on open: `CodeFileDocument.decode` gained a plausibility pre-check (4KiB pair sampling) so interleaved-NUL UTF-16 is no longer misread as UTF-8; the lifecycle test matrix was extended to 10 tests, including bomless UTF-16 cases.
- Fixed a stale `CodeEdit.xcodeproj` reference in the README.
- Updated docs/CODE_ARCHITECTURE.md and docs/DEVELOPER_USAGE.md for WP-Q1: documented the syntax-highlight pipeline's public parse/interpret/span-map stages, the TokenRun/HighlightSpan data types crossing stage boundaries, the FirstCharFilter and CompiledRegexCache performance seams, and the scripts/highlight_benchmark.sh per-stage timing tool.
- Fixed `devel/clean_build.sh` deleting `.build` wholesale on the Swift path, which forced a full SwiftPM dependency re-fetch (`.build/checkouts`, `.build/repositories`) on every light clean; it now targets only compiled output (`.build/debug`, `.build/release`, `.build/artifacts`, `.build/build.db`, the `*-apple-macosx` triple directories, and the top-level `*.yaml` build-plan files), keeps `.build/workspace-state.json` and `.swiftpm`, and documents the `swift package clean` vs `swift package reset` distinction it mirrors; `devel/dist_clean.sh`'s header now notes Swift dependencies re-fetch automatically on the next build after its full wipe.
- Refreshed `README.md`: new About-ready first paragraph, curated Documentation section linking the core docs (CODE_ARCHITECTURE, FILE_STRUCTURE, INSTALL, USAGE, SCOPE, DEVELOPER_USAGE) by convention, and removed the stale INSTALL/USAGE/TROUBLESHOOTING absence note; screenshot-docs managed block left untouched.
- Fixed staleness in `docs/TODO.md` and `docs/RELATED_PROJECTS.md` left over from the WP-P5 dead-code purge: removed the three `docs/TODO.md` items pointing at deleted files (`Loopable.swift`, `Limiter.swift`, `CodeEditKeychain.swift`); moved `docs/RELATED_PROJECTS.md`'s AboutWindow and CodeEditSymbols entries out of "Confirmed related projects" into "Commonly confused unrelated projects" (both packages were deleted alongside the About window feature) and reconciled the Evidence notes summary against the current four-dependency `Package.swift`.
- Refreshed `docs/CODE_ARCHITECTURE.md` and `docs/FILE_STRUCTURE.md` against the surviving post-deletion tree: documented the AboutWindow and CodeEditSymbols build-dependency packages, clarified CodeEditSourceEditor is retained only as WP-F1 harvest source (not built), listed previously-undocumented root entries (`scripts/`, `tests/`, `AppCast/`, `ThirdParty/`), and completed the documentation map and generated-artifacts list.
- Refreshed `docs/RELATED_PROJECTS.md` into the standard Confirmed/Possible template shape: added CodeEditSymbols and KDE syntax-highlighting (KSyntaxHighlighting) as newly evidenced Confirmed entries, reclassified CodeEditSourceEditor as harvest-only (not a Package.swift dependency), and moved CodeEditKit/WelcomeWindow to a "commonly confused unrelated projects" section noting their 2026-07-09 deletion.
- Expanded `AGENTS.md` doc pointers to cover the newly landed `docs/INSTALL.md`, `docs/USAGE.md`, `docs/FILE_FORMATS.md`, `docs/THEME_FORMAT.md`, `docs/RELATED_PROJECTS.md`, `docs/TROUBLESHOOTING.md`, `docs/ROADMAP.md`, `docs/TODO.md`, `docs/NEWS.md`, and `MILESTONE3_CHECKLIST.md`.
- Fixed Swift 6 actor-isolation warnings in `Packages/CodeEditTextView/Sources/CodeEditTextView/Utils/ViewReuseQueue.swift`: annotated the class `@MainActor` (it only ever manages `NSView` instances) and its `deinit` as `isolated deinit`, since `TextLayoutManager`, the only caller, is already `@MainActor`.
- WP-P6: burned down the from-scratch `swift build -c debug` warning count in `Packages/` from 7 distinct warning sites (the plan's ~276 estimate turned out to be stale/inflated by a reused build-path collision with a concurrent build) to 0. Fixes: dropped a dead `let maxWidth = typesetter.typeset(...)` binding in `TextLine.swift` (the call returns `Void`) and a dead `relativeMaxY` binding in `TextLayoutManager+Layout.swift` (only referenced from commented-out code); changed an unmutated `var attachments` to `let` in `TextView+Insert.swift`; annotated `CursorTimer` `@MainActor` with `isolated deinit` (its only caller, `TextSelectionManager`, is already `@MainActor`, following the `ViewReuseQueue` template) and wrapped its `Timer.scheduledTimer` closure body in `MainActor.assumeIsolated` (the timer is always scheduled from this now-main-actor type, so it always fires on main); annotated the `TextLayoutManagerRenderDelegate.lineFragmentView(for:)` protocol requirement and its default implementation `@MainActor` (the only in-build caller, `TextLayoutManager`, is already `@MainActor`). Also fixed a bonus `String(cString:)` deprecation warning in `Packages/CodeEditSyntaxDefinitions/Tests/CodeEditSyntaxDefinitionsTests/KateInterpreterBenchmarkTests.swift` (not part of the root build's warning count, since dependency-package test targets aren't in the root build graph) by truncating at the null terminator and decoding via `String(decoding:as:)`. `swift test` (40 tests, 7 suites) stayed green throughout. Left untouched as needing real redesign, not mechanical fixes: `Packages/CodeEditLanguages/Tests/CodeEditLanguagesTests/CodeEditLanguagesTests.swift:10` imports `SwiftTreeSitter`, a module never declared as a package dependency; `Packages/CodeEditTextView/Tests/CodeEditTextViewTests/KillRingTests.swift:70-71` calls a `@MainActor`-isolated `TextView` from a non-isolated XCTest method (neither test target builds today, and neither is reachable from the root package's build graph or `swift test`).
- WP-F2 quality-review follow-up: `ThemeRepository.resolvedTheme(named:)` was performing bundle discovery, disk reads, and a full parse on every call, including `PlainSyntaxHighlighter`'s synchronous cached-span fast path -- per-keystroke disk I/O on the path the M8 milestone must bring under a 16 ms p95. Added a lock-guarded, process-wide `resolvedThemeCache` (an `@unchecked Sendable` class keyed by `(requestedName, overrideRoot, bundle)`), mirroring `SyntaxDefinitionRepository`'s and `CompiledRegexCache`'s existing lock-guarded lazy-loading pattern in `CodeEditSyntaxDefinitions`; results are reused until the new `ThemeRepository.invalidateCache()` is called (named for a future Settings live-apply action). Added `resolvedThemeCachesUntilInvalidated` proving a resolved theme survives deletion of its backing user file until `invalidateCache()` forces fresh discovery.
- `EditorCommandRouter`'s `copy()` and `paste()` now delegate to `TextView`'s own copy/paste implementations (which are multi-cursor aware) instead of reimplementing `NSPasteboard` logic, letting the router drop its AppKit import entirely; found by the WP-S2 spec review.
- Smoke gate revalidation (`scripts/plain_editor_smoke.sh`): removed a stopped agent's rejected `defaults write org.vosslab.SwiftlyCodeEdit`/`defaults delete` theme-seeding block (a prior agent was halted mid-task for mutating the real preferences domain) along with its now-unused `scripts/smoke_fixtures/smoke_theme_probe.yaml` fixture, keeping WP-S3's reviewed `SHELL=SwiftUI` gate and per-menu `check_menu_has_items` assertions intact. Extended the command self-test hard gate to WP-F4 patch 2's full 15-key line (`cleanText`/`cleanUndo`/`cleanRedo` plus the four Clean Text sub-cleaners: `cleanLineEndings`, `cleanFinalNewline`, `cleanTabsToSpaces`, `cleanSpacesToTabs`, `cleanSmartPunct`). Fixed the screenshot deletion hazard from `docs/TODO.md`: capture now writes to a `mktemp` scratch path and only `mv`s over the git-tracked `docs/screenshots/codeedit_window.png` after a confirmed non-empty capture, so a TCC-denied or otherwise failed capture leaves the tracked file untouched instead of deleting it ahead of time. Guarded `check_menu_has_items`'s `extract_menu_section` assignment with `|| true` so the missing-section diagnostic is reachable under `set -e` (WP-S3 spec-review finding), verified against a doctored-log fail-path probe covering missing-section, missing-item, and all-present cases. Investigated the WP-F5 `SETTINGS_APPLIED` smoke assertion empirically (launching the debug build with seeded `-PlainEditor.fontSize`/`-PlainEditor.themeName` launch arguments, which land in `NSArgumentDomain` and are per-process/volatile, never touching the real preferences domain) and found neither the font nor theme marker can fire from a seeded cold launch under the current design: the font markers compare against a value already set identically at view creation, and the theme marker's only bundled alternate name (`ThemeRepository` ships just `standard`) means an unknown seeded theme name falls back to the same name the highlighter already starts from; a real second theme would require writing into the user's actual Application Support directory, the same class of pollution as the rejected `defaults write` approach. Filed the concrete DEBUG-only Swift seam needed (a `CODEEDIT_SETTINGS_APPLY_SELF_TEST`-style env var driving a post-mount live change) in `docs/TODO.md` rather than asserting a marker that can never fire; the smoke script does not gate `SETTINGS_APPLIED` yet. Verified: `./scripts/plain_editor_smoke.sh --no-screenshot` -> `SMOKE_EXIT=0` with every new gate active; `pytest tests/test_ascii_compliance.py tests/test_markdown_links.py` 785 passed; `defaults read org.vosslab.SwiftlyCodeEdit` identical before and after every run in this session (only the pre-existing `NSWindow Frame` key).
- WP-Q9: replaced the O(cursor-offset) cursor-label scan with a binary search into a cached
  `lineStartIndex` (`PlainEditorStatusReporter.lineStartIndex(in:)`/
  `lineNumber(forOffset:lineStartIndex:)`), refreshed alongside WP-Q2's cached total-line count on
  the debounced heavy recompute. Fixing this exposed a stale-cache bug in the WP-Q2
  `CursorSignature` dedup: it keyed the skip signature only on `(location, length, documentLength,
  totalLines, editGeneration)`, so an equal-length edit that relocates a line break before the
  cursor (reachable through Find/Replace's equal-length regex substitution) produced a
  bit-identical signature and left a wrong cursor label at rest even after the debounce refreshed
  the cache. Fixed by dropping the signature and `editGeneration` dedup entirely rather than
  adding another key: the binary-search scan is O(log lines) and cheap enough to recompute
  unconditionally on every call, so no signature can go stale if there is none. Added
  `cursorLabelCorrectsStaleCacheOnDebouncedRecompute` (via a new DEBUG-only
  `drainHeavyRecomputeForTesting` test hook that drives the debounce body synchronously) and
  extended `cursorLabelWithCachedIndexMatchesStatelessOracle` with a CRLF sample.

### Removals and Deprecations

- WP-P1 chunk 1: deleted every `CodeEdit/`-scoped Package.swift-excluded tree (559 files): the full-directory excludes (`Features/{ActivityViewer,CEWorkspace,CEWorkspaceSettings,CodeEditUI,Commands,Extensions,Feedback,InspectorArea,LSP,NavigatorArea,Notifications,OpenQuickly,Search,Settings,SplitView,SourceControl,StatusBar,Tasks,TerminalEmulator,UtilityArea,Welcome,WindowCommands}`), the partial excludes (`Features/Documents/{Controllers,Indexer,WorkspaceDocument}`, `Features/Editor/{JumpBar,Models,TabBar}`, four `Features/Editor/Views/*.swift` files), the root-level dead files (`AppDelegate.swift`, `CodeEdit.entitlements`, `Info.plist`, `Preview Content`, `World.swift`, `WorkspaceSheets.swift`, `WorkspaceView.swift`, `ShellIntegration`), and `Utils/{Compatibility,DependencyInjection,Environment,ShellClient,withTimeout.swift,Extensions/{Date,LanguageIdentifier,LocalProcess,SemanticToken,TextView,URL}}`; removed the now-empty `exclude:` array from `Package.swift` (59 entries dropped to 0). Gate: `./build_debug.sh` (Build complete!, 4.77s) and `./scripts/plain_editor_smoke.sh` (SMOKE_EXIT=0).
- WP-P1 chunk 2: deleted the legacy test and UI-test trees no longer reachable from any SwiftPM target (56 files): `CodeEditTests/Features/`, `CodeEditTests/Utils/` (both outside the live `CodeEditTests/PackageSmoke` testTarget path), and `CodeEditUITests/`. Gate: `./build_debug.sh` (Build complete!, 2.35s) and `./scripts/plain_editor_smoke.sh` (SMOKE_EXIT=0).
- WP-P1 chunk 3: deleted the dead root-level helper-app directories (19 files) `Configs/` and `OpenWithCodeEdit/`, confirmed unreferenced by `Package.swift`, `build_debug.sh`, `build_release.sh`, and `scripts/`. Gate: `./build_debug.sh` (Build complete!, 2.49s) and `./scripts/plain_editor_smoke.sh` (SMOKE_EXIT=0). Final gates after all three chunks: `swift test` (40 tests, 7 suites, all passed) and `pytest tests/` (2913 passed).
- WP-P2: deleted the unused local packages `Packages/CodeEditKit` (44 files) and `Packages/WelcomeWindow` (43 files); neither was a `Package.swift` dependency and `grep` across `CodeEdit/`, `CodeEditTests/`, `CodeEditUITests/`, `Packages/`, and `devel/` found zero live source references to either package name (the only `WelcomeWindow` hits were stale file-header comments in `Packages/AboutWindow`, not imports). `Package.resolved` was already unaffected (local path dependencies are not recorded there) and its checksum was unchanged after `./build_debug.sh`. Gates: `./build_debug.sh` (Build complete!), `swift test` (40 tests, 7 suites, all passed), `./scripts/plain_editor_smoke.sh` (SMOKE_EXIT=0).
- WP-P5 chunk 1: deleted the dead About window feature per `docs/active_plans/audits/live_target_dead_code_audit.md` (10 files under `CodeEdit/Features/About/`, zero external references), plus the two package dependencies it orphaned, `Packages/AboutWindow` (46 files) and `Packages/CodeEditSymbols` (41 files), removing their four `Package.swift` dependency/product lines. No About menu item existed anywhere in the compiled app (`grep -rn "About" CodeEdit/ --include=*.swift` outside the deleted subtree returned 0 hits before deletion), so there was no dangling menu item to remove; the app already relies on nothing for an About panel. Gates: `./build_debug.sh` (Build complete!, 6.24s) and `./scripts/plain_editor_smoke.sh` (SMOKE_EXIT=0).
- WP-P5 chunk 2: deleted the dead Keybindings feature (`CommandManager.swift`, `KeybindingManager.swift`, `ModifierKeysObserver.swift`, `default_keybindings.json`, with the matching `Package.swift` resource line removed), the dead KeyChain cluster (`CodeEditKeychain.swift`, `CodeEditKeychainConstants.swift`, `KeychainSwiftAccessOptions.swift`), and the dead SmokeTesting App Intents file (`PlainEditorSmokeIntents.swift`) -- 8 files total, all zero-external-reference per the audit (the hand-built AppKit menu never invokes `CommandManager.shared`/`KeybindingManager.shared`, and the live smoke path is env-var/screenshot driven, not App-Intents driven). Gates: `./build_debug.sh` (Build complete!, 5.07s) and `./scripts/plain_editor_smoke.sh` (SMOKE_EXIT=0).
- WP-P5 chunk 3: deleted the dead `CodeEdit/Utils` tree down to its three live survivors (`DebugRuntimeLog.swift`, `String+Lines.swift`, `SceneID.swift`): 5 standalone Utils types (`RegexFormatter`, `TrimWhitespaceFormatter`, `Limiter`, `Loopable`, `SearchableSettingsPage`) and 27 Utils extensions/helpers (Array, Bundle, Collection, Color, FileManager, Int, NSApplication, NSTableView, NSWindow, OperatingSystemVersion, ten String extensions including `String+MD5`/`String+SHA256`/`String+Ranges`, Text, View, ZipFoundation, `FocusedValues.swift`), plus the unrelated dead `CodeEdit/WindowObserver.swift` (zero external references) -- 33 files total. Gates: `./build_debug.sh` (Build complete!, 4.31s) and `./scripts/plain_editor_smoke.sh` (SMOKE_EXIT=0).
- WP-P5 chunk 4: deleted the two MEDIUM-confidence audit items, `CodeEdit/Localization/Localized+Ex.swift` and the generic-name String extensions (already covered by chunk 3's `String+MD5`/`String+Ranges`/`String+SHA256`), confirmed by the same green build gate with no reclassification needed; also deleted the stray out-of-compile-surface `CodeEditUI/src/Preferences/ViewOffsetPreferenceKey.swift` (its directory's only tracked file) and the whole `AppCast/` Sparkle-feed config tree (8 files), both unreferenced by `Package.swift`. Removed the dead in-file `PlainEditorCommands` SwiftUI `Commands` struct (`CodeEditApp.swift:440-545`, 106 lines) -- unreachable because the app is a hand-built AppKit `@main enum` with no `.commands { }`/`App`/`Scene` consumer -- and collapsed `PlainSyntaxTheme.current` to always return `.standard`, deleting the legacy `SYNTAX_THEME_VARIANT=="rotated"` branch and the now-unused `PlainSyntaxTheme.rotated` variant (nothing in `scripts/` or `tests/` ever sets that env var). Gates: `./build_debug.sh` (Build complete!, 4.04s) and `./scripts/plain_editor_smoke.sh` (SMOKE_EXIT=0).
- WP-P5 chunk 5 (sweep addition): deleted the `Documentation.docc/CodeEditUI/` DocC subtree (8 files: 4 markdown pages plus 4 `Resources/` screenshots), a top-level tree sibling to the already-deleted `CodeEditUI/src/` stray, outside the SwiftPM compile surface and unreferenced by `Package.swift`/`build_debug.sh`/`build_release.sh`/`scripts/`. Removed the now-dangling `### CodeEditUI` / `<doc:CodeEditUI>` section from `Documentation.docc/Documentation.md`'s topic index; left that index's other pre-existing stale sections (About Window, Settings, Feedback, Keybindings, and similar, already dead before this WP) untouched as out of scope. Gates: `./build_debug.sh` (Build complete!, 3.91s) and `./scripts/plain_editor_smoke.sh` (SMOKE_EXIT=0).
- WP-P5 chunk 6 (sweep addition): deleted the whole remaining `Documentation.docc/` tree (26 files) after confirming `grep -n "Documentation.docc" Package.swift build_debug.sh build_release.sh` and `grep -rn "Documentation.docc" scripts` both returned zero hits and `Package.swift` declares no DocC plugin; every remaining section (About Window, App Window, AppPreferences, FileManagement, Git, KeyChain, Keybindings, Welcome, plus the top-level `Documentation.md` index) indexed only symbols already deleted by WP-P1/P2 or the earlier chunks of this WP. Gates: `./build_debug.sh` (Build complete!, 3.11s) and `./scripts/plain_editor_smoke.sh` (SMOKE_EXIT=0). Final gates after all six chunks: `swift test` (40 tests, 7 suites, all passed, including `UndoManagerRegistrationTests`) and `pytest tests/` (2398 passed; the count moved from 2418 because deleting 8 fewer files in chunk 5 reduces the file-parametrized `test_markdown_links`/`test_ascii_compliance` cases, not a failure -- an earlier `test_markdown_links` failure from a stale `docs/CODE_ARCHITECTURE.md` "Known gaps" bullet pointing at the just-deleted `ViewOffsetPreferenceKey.swift` was fixed by deleting that bullet).
- WP-F2: deleted the hardcoded `private struct PlainSyntaxTheme` from `PlainSyntaxHighlighter.swift` (its `standard` palette, `color(for:)` resolution, and the `.current` accessor). `PlainSyntaxHighlighter.applyHighlight` now resolves colors through `ThemeRepository.resolvedTheme(named:)` and `ThemeVariant.color(forToken:styleName:)`, which preserves the same resolution order (style key, then token key, then `base_text`) and picks the light or dark variant from `NSApp.effectiveAppearance.bestMatch(from: [.aqua, .darkAqua])`.
- WP-F1 patch 19: deleted the vendored `Packages/CodeEditSourceEditor/` tree (199 tracked files, `git rm -r`) now that patch 18 finished porting its find/replace panel into `CodeEdit/Features/Find/`. `Package.swift` never declared the package as a dependency (confirmed by a pre-deletion grep) and the root `Package.resolved` carried no reference to it, so neither file needed a change. Confirmed zero remaining code references anywhere in `CodeEdit/`, `CodeEditTests/`, or the other `Packages/` trees -- the only hits were pre-existing comment/README mentions naming the upstream project by name (four `CodeEdit/Features/Find/*.swift` history comments and `CodeEditTextView`'s own migration-notice comments/README links), not imports. Gates: `swift build` (Build complete!, no warnings), `swift test` (119 tests, 13 suites, 4 known issues, unchanged from patch 18), `pytest tests/test_ascii_compliance.py tests/test_markdown_links.py` (771 passed). Updated `docs/RELATED_PROJECTS.md`, `docs/FILE_STRUCTURE.md`, and `docs/CODE_ARCHITECTURE.md` to record the package as harvested-and-deleted rather than harvest-source-pending-deletion.
- WP-S4: deleted the retired AppKit shell content from `CodeEdit/CodeEditApp.swift` (455 lines
  down to 40): removed `CodeEditMain.main()`, `.appDelegate`, `PlainEditorAppDelegate`,
  `PlainEditorActionRouter`, `PlainEditorMainMenu`, and the `TextView.cleanText` extension; kept
  only the `launchStartNanoseconds`, `killAfterSeconds`, `logLaunchToWindowIfNeeded`, and
  `didLogLaunchToWindow` members the live SwiftUI shell and document bridge still call for the
  `LAUNCH_TO_WINDOW_MS` marker.

### Decisions and Failures

- WP-Q6 measurement + fixed-cost investigation: the keystroke-latency gate (p95 < 16 ms) is still MISSED after the bounded-rehighlight work, and the remaining floor is NOT the highlighter. Candidate comparison (200 edits, ~1 MB fixture, MacBookPro18,3, recorded at `test-results/perf/keystroke_latency_candidates.txt`): edited-line-window (shipped default) min 1977.62 / median 2023.05 / p95 2057.97 ms (complete); visible-range (candidate) min 2056.21 / median 12607.85 / p95 12936.91 ms over 191/200 edits before the harness's 2400 s poll deadline (visible-range keeps its region pinned at the top viewport `{0,502}` so an off-screen edit is never repainted and additionally pays large, variable 1 MB relayout/scroll cost -- confirming the edited-line-window ship choice); true pre-Q6 baseline (git 028868f) min 2627.47 / median 10662.09 / p95 15233.04 ms. Root cause of the flat ~2 s floor, classified as a genuine accidental delay (not a bench artifact, not a debounce), but in a subsystem WP-Q6 does not own: the status bar recomputes whole-document statistics on every keystroke. `PlainEditorChromeModel.refresh` (`CodeEdit/Features/Editor/Views/CodeFileView.swift:508`) runs roughly eight O(n) passes over the 1 MB text per call via `PlainEditorStatusReporter` (`CodeEdit/Features/Editor/PlainEditorStatusReporter.swift` -- `cursorLabel:5` does two full `components(separatedBy:.newlines)` splits, one over a ~1 MB substring-to-cursor; `wordCount:15` a Unicode-aware full scan; `indentationLabel:20` and `lineCount` at `CodeFileView.swift:514` more full splits; `lineEndingLabel:44` up to two `contains` scans; plus a full `NSTextStorage`->`String` bridge) and it fires ~3x per keystroke (onTextChange + onSelectionChange) synchronously on the main actor, serializing ahead of the now-cheap bounded highlight Task. This is the M8 "incremental status bar" work (`CodeFileDocument.swift:135`), outside the highlighter/bench/harness WP-Q6 owns, and is escalated rather than fixed here. Baseline-clobber note: `tests/e2e/e2e_keystroke_latency.py --record-baseline` overwrites `test-results/perf/keystroke_latency.txt` on every run, so that file now holds the post-fix edited-line-window numbers (min 1977.62 / median 2023.05 / p95 2057.97); the durable side-by-side comparison against the true baseline lives in `keystroke_latency_candidates.txt`.
- Corrected milestone checklist claims in place with `Corrected 2026-07-09:` annotations, covering the dead Find menu, dark mode never captured, overstated theme-awareness, wrong-engine test coverage, and a silent-blank encoding case mislabeled as a handled limitation.
- Noted that the entire package-smoke test suite had never been in git history due to the `.gitignore` pattern.
- Accepted WP-V3 decode-fallback breadth and recorded it in the plan: the Windows-1252 fallback rejects only 5 byte values, with BOM-less UTF-16 coverage owed in WP-S3.
- Accepted WP-V4 quality nits as polish debt: `SMOKE_EXIT` is not printed on pre-launch usage errors, and a malformed `--kill-after` value silently disables the backstop.
- Investigated a spec-review must-fix alleging an unsynchronized data race in `SyntaxDefinitionRepository` and found it already closed in HEAD: an `NSLock` guards all mutable state. Kept the internal lock instead of converting to an actor, because an actor would force async through the highlight API for no correctness gain.
- WP-Q4 first metric counted 15 "significant colors" that were mostly grayscale antialiasing ramps; reworked to a hue-family metric so the gate proves chromatic syntax coloring, not gray shades.
- WP-Q0b quality review accepted two polish items into WP-Q1: an iteration cap on the drift-recompute loop, and a comment on the compute task's intentional bounded strong capture of the storage.
- Resolved the `Packages/CodeEditHighlighting` open question as KEEP: the WP-P1 scout confirmed live dependence (`HighlightSpan` used by `PlainSyntaxHighlighter.swift` and `CodeFileView.swift`) via a nested dependency in `Packages/CodeEditSyntaxDefinitions/Package.swift`.
- WP-P3 spec review flagged "CodeEdit" strings in Feedback/WindowCommands/Settings/SourceControl trees as MUSTFIX; overruled with evidence, since all live in Package.swift-excluded, never-compiled trees already on the WP-P1 deletion list; the compiled-files-only audit scope stands.
- WP-S0b spike verdict FAIL: SwiftUI `TextEditor`+`AttributedString` cannot meet editor gates (caret collapses to EOF on any programmatic attribute write; keystroke p95 140.56 ms against a 16 ms gate; a 1 MB document wedges the main thread). The TextKit bridge stays as the replaceable AppKit adapter under the SwiftUI-first principle, to be re-evaluated against the next macOS SDK. Decision record: `docs/active_plans/decisions/text_engine_decision.md`.
- WP-S0c re-validation verdict FAIL confirmed - TextKit adapter stands: re-ran three of the four original WP-S0b editor gates (keystroke p95, span apply, cursor preserved) against SwiftUI `TextEditor`+`AttributedString` on the current toolchain (Swift 6.3.3, macOS 26.5.2, MacBookPro18,3) plus a newly formalized large-mount gate (~1 MB document mounts within 5 s, replacing the original's informal 25 s wedge observation); `scroll_preserved`, which passed in the original spike, was not re-tested. The three carried-over gates failed again - keystroke p95 513.30 ms vs 16 ms gate, span apply 237.38 ms vs 50 ms gate, caret still collapses to end-of-document on both batched and in-place attribute writes - and the formalized large-mount gate also failed, with a ~1 MB document still wedging the main thread past the 5 s window. Numbers were worse than the original spike, not better, so the SwiftUI-native interior stays deferred. Re-validation section appended to `docs/active_plans/decisions/text_engine_decision.md`.
- Deferred large-file (~1 MB+) cold-highlight performance (WP-Q1): these files remain multi-second cold until viewport-first highlighting lands, deferred to a future large-file work package.
- WP-S0 document architecture spike verdict: keep `NSDocument` (`CodeFileDocument`) behind a `DocumentGroup` bridge, not a SwiftUI-native `ReferenceFileDocument`. A throwaway `DocumentGroup`+`ReferenceFileDocument` prototype (measured on macOS 26.5.2, Swift 6.3.3, MacBookPro18,3) passed the two encoding gates (Save As and open-edit-save round-trips are byte-identical across UTF-8, UTF-16 BE/LE, Windows-1252, Latin-1) but failed the two lifecycle gates: a genuinely dirty `ReferenceFileDocument` never autosaved within 8 s and exposes no `scheduleAutosaving` override for the 2 s debounce, and an external file change either produced no in-place reload or a fresh document instance with a new `NSTextStorage`, never a write into the storage the views held. Mechanical rule (any gate failure selects the bridge) gives the NSDocument-behind-DocumentGroup verdict; the single sanctioned document-layer AppKit bridge file is `CodeFileDocumentBridge.swift` (created by WP-S1), re-evaluated at the next macOS SDK. Decision record: `docs/active_plans/decisions/document_architecture_decision.md`.
- Architect resolved the WP-S0 bridge-mechanism fork: host document windows through `NSDocumentController` under a plain SwiftUI `App`, not a `ReferenceFileDocument` facade under `DocumentGroup`. `DocumentGroup` would wrap the facade in its own private `NSDocument`, giving two document owners and two `NSFilePresenter` registrations per URL, and its private document is exactly what failed gate 1 (no `scheduleAutosaving` debounce) and gate 3 (reload recreates the `NSTextStorage`, breaking `===`). A single `NSDocumentController` keeps `CodeFileDocument` the sole owner of autosave debounce, `NSFilePresenter` reload, and shared-storage identity. Undo-manager ownership is bypass: the hosted SwiftUI tree does not set `\.environment(\.undoManager)`; `CodeFileDocument.undoManager` wired to the `TextView` is the single owner. Addendum in `docs/active_plans/decisions/document_architecture_decision.md`.
- M8 keystroke gate escalation resolved by architect as a discovery outcome, not
  a relaxation: the plan's 16 ms-at-1 MB target is an untested 60 fps frame-budget
  guess, not a number derived from measured TextKit behavior. The WP-Q2 shipped
  run measures keystroke p95 223 ms on the 1 MB / 44,884-line fixture, a ~68x
  improvement over the ~15,233 ms pre-M8 baseline but a ~14x miss of 16 ms.
  Attribution (`/tmp/codeedit_runtime.log`) shows both first-party subsystems
  already bounded (bounded rehighlight region ~1,810 chars, `STATUS_REFRESH_MS`
  ~12 ms) and the dominant ~150 ms residual is TextKit mutation plus relayout on
  the `NSTextStorage`, an engine floor at 44,884 lines that the milestone does
  not own and cannot remove without a text-engine replacement already measured
  worse (WP-S0b, WP-S0c). Decision: option (b), a gate derived from a measured
  latency-versus-size curve rather than asserted up front -- retain absolute
  16 ms p95 up to the measured crossing point `N` (chars and lines), and above
  `N` use the measured degradation curve plus the retained regression threshold
  (no more than 20 percent above 223 ms and at least 10x better than baseline) --
  justified against the measured TextKit floor and the `docs/SCOPE.md`
  clean-plain-text-editor goal and the Kate precedent. A Kate-style
  large-document highlight and status cutoff is evaluated and noted to not reach
  16 ms on its own, because the dominant residual is layout, not highlighting; it
  is filed as an optional product-gated UX guard whose ceiling comes from the
  same sweep. M8 does not exit now; it exits under the derived gate once WP-Q7
  (latency-versus-size sweep to measure `N`) and WP-Q8 (curve-derived gate
  wiring) land, with WP-Q9 (fix the O(cursor-offset) cursor-label scan at
  `PlainEditorStatusReporter.swift:25`) recommended in scope. Decision record:
  [m8_keystroke_gate_decision.md](active_plans/decisions/m8_keystroke_gate_decision.md).
- M8 keystroke gate decision revised: the ship gate now measures the synchronous first-paint
  slice (`KEYSTROKE_MUTATION_MS`), not full highlight settle (`KEYSTROKE_MS`). WP-Q8 phase
  attribution showed the prior 16 ms gate was applied to the wrong metric -- perceived typing
  latency (edit apply plus status refresh plus synchronous scheduling) measures roughly
  1.2-2.1 ms p95 across tested sizes including the 1 MB fixture, while `KEYSTROKE_MS` conflated
  that with an async ~7-9 ms scheduling hop and DEBUG-inflated paint. Highlight settle is now a
  separately tracked background-freshness metric, not a ship gate. Decision record:
  [m8_keystroke_gate_decision.md](active_plans/decisions/m8_keystroke_gate_decision.md).

### Developer Tests and Notes

- M8 entry-criterion sufficiency check: traced all six required `EditedTextChange` cases (typing, paste, undo, redo, reload, Clean Text) and confirmed each broadcasts a usable payload. The open question was whether Clean Text's whole-buffer rewrite (`EditorCommandRouter.applyCleanTransform`/`CodeEditApp.cleanText()`) bypasses the notification the way reload does; it does not -- Clean Text calls `textView.replaceCharacters(in:with:)`, the same mutation API typing/paste/undo/redo use, so it fires `didReplaceContentsIn` and broadcasts a bounded `.range` (covering the whole pre-edit buffer), not `.fullInvalidation`. Flagged a doc-comment inaccuracy on `CodeFileDocument.EditedTextChange.fullInvalidation` (names "Clean Text" alongside reload, but only reload broadcasts that case) as optional follow-up, not a functional gap. Added `CodeEditTests/PackageSmoke/EditedRangeContractTests.swift` pinning the exact Clean Text broadcast shape. Verdict table and evidence: `docs/active_plans/audits/edited_range_sufficiency_check.md`. `swift test` 130 tests in 15 suites pass with 3 known issues (unrelated concurrent-lane baseline failures); `pytest tests/` 1974 passed.
- Ran a read-only document lifecycle audit ahead of milestone MS and found 4 HIGH findings (undo never clears the dirty flag; a dirty document's external change is silently dropped, a lost-update; reload swallows decode errors via `try?`; reload does not reset the undo stack), 2 MEDIUM, and 2 LOW findings; also confirmed `UndoManagerRegistration.swift` is dead code. Report: `docs/active_plans/audits/document_lifecycle_audit.md`.
- Wrote the document state contract (saved-text identity, file-modification-date advancement rules, dirty-flag semantics, undo-history-across-save/reload rules, and the full external-change matrix) into `docs/active_plans/decisions/document_architecture_decision.md`, and added four expected-fail Swift Testing regression tests in `CodeEditTests/PackageSmoke/CodeFileDocumentLifecycleGapTests.swift` pinning the audit's four HIGH findings against today's code, each wrapped in `withKnownIssue` with its exact baseline failure recorded: `undoToSavedContentClearsDirtyFlag` (closes WP-L1, fails on `isDocumentEdited == false` after undo-to-saved-content), `externalChangeWithUnsavedEditsSurfacesConflict` (closes WP-L2, fails on `fileModificationDate` staying stale after a dirty-document external change), `undecodableExternalChangeSurfacesError` (closes WP-L3, fails on `fileModificationDate` silently advancing on an undecodable reload), and `externalReloadInvalidatesStaleUndoOperations` (closes WP-L4, fails on the reloaded buffer being corrupted by a stale pre-reload undo operation). The WP-L4 test deliberately keeps the stale operation's offsets in-bounds after reload; an out-of-range replay instead raises an uncaught `NSException` that terminates the whole test process, which Swift Testing cannot pin as a known issue. `swift test` reports 44 tests in 8 suites passing with 4 known issues; `pytest tests/` stays green (2294 passed). The file-deleted-or-moved row of the external-change matrix is documented as a residual gap: it needs `NSDocumentController`/`NSFilePresenter` machinery not reachable from a package test, so it is left for an E2E test per `docs/E2E_TESTS.md`.
- Ran a read-only live-target dead-code audit inside the SwiftPM compile surface and produced `docs/active_plans/audits/live_target_dead_code_audit.md`: 50 dead whole files + 2 in-file symbol removals, plus 2 orphaned package dependencies (AboutWindow, CodeEditSymbols) reachable only from a dead About subtree; the whole `CodeEdit/Utils` tree is dead except `DebugRuntimeLog.swift`, `String+Lines.swift`, and `SceneID.swift`.
- Logged a smoke-coverage gap in `docs/TODO.md` found by the WP-P5 reviewer: `scripts/plain_editor_smoke.sh` exercises the in-memory command self-test lifecycle but has no literal save-to-disk/reopen round-trip, so save-path regressions are invisible to smoke.
- WP-Q5 baseline run 4 (discarded, not the recorded baseline) hit the keystroke-latency harness's 900 s poll deadline at 42/200 edits, timed out against `ThemeRepository.resolvedTheme(named:)` still re-reading and re-parsing the theme YAML from disk on every highlight pass -- before its resolved-theme cache (see the WP-F2 quality-review follow-up above) landed mid-run. Measured average ~12.9 s/edit (540.6 s across 42 edits, individual values ranging ~2.4 s-14.8 s) versus the ~2.4 s/edit the recorded baseline (run 5, after the cache fix landed) shows on the same fixture -- direct evidence of how much the uncached theme-load step alone was costing the per-keystroke highlight path, recorded in `test-results/perf/keystroke_latency.txt` as input for M8.
- WP-S4: renamed `PlainEditorClipboardTests.swift` to `EditorCommandRouterClipboardTests.swift`
  (`git mv`) and ported its three cases off the just-deleted `PlainEditorActionRouter` onto the
  live `EditorCommandRouter`, including the external-pasteboard regression guard; renamed the
  empty-pasteboard case to `pasteIsNoOpWhenPasteboardIsEmpty` since
  `EditorCommandRouter.paste()` always returns true once dispatched, unlike the retired router's
  `paste()`, which returned false on an empty pasteboard.
- Floor-attribution instrumentation (WP-Q8): added DEBUG-only per-edit phase-timing markers.
  `PlainEditorKeystrokeBench.swift` now logs `KEYSTROKE_MUTATION_MS`, the synchronous
  edit-apply-plus-status-refresh slice that ends before any async highlight hop runs;
  `PlainSyntaxHighlighter.swift` and `PlainSyntaxHighlightFullPass.swift` log
  `KEYSTROKE_SCHED_MS`/`KEYSTROKE_SPAN_MS`/`KEYSTROKE_PAINT_MS` for the async
  main-actor-enqueue, off-main span-compute, and paint sub-phases, gated to fire only from the
  first bench edit on (the cold-open pass stays unmarked) so the markers never fire outside a
  bench run.

## 2026-07-07

### Fixes and Maintenance

- Fixed the merged syntax-definition package layout so SwiftPM processes only XML files from the active Kate vendor resource tree and the live highlighter loads Swift XML from that same bundle path.
- Restored visible syntax highlighting by keeping the XML-based Kate colors applied through the final smoke view state, and restored the historical filename-based screenshot capture command.
- Changed fresh plain-editor document windows to a 960 x 600 landscape default and made the command self-test restore the original selection instead of leaving Select All highlighted in screenshots.
- Made the distribution-clean debug build path recreate SwiftPM's artifact directory, silence false-positive clean-script `find` output, and route `CodeEditTextView` viewport notifications through main AppKit selectors.
- Replaced the direct SwiftPM launch entry with an AppKit application main for the plain-editor path so the smoke harness can exercise the same file-backed document window lifecycle without relying on a SwiftUI settings-only scene.
- Restored the full package-smoke test target source set so SwiftPM includes every `CodeEditTests/PackageSmoke` test file instead of warning about unhandled tests.
- Added a context-preserving Kate XML Swift syntax highlighter and wired it into the live plain-editor path so Swift files receive semantic color spans in `NSTextStorage`.
- Added smoke-log validation requiring Swift comment, keyword, number, string, and type highlighting with multiple distinct readable colors in the active editor.
- Added focused Swift Testing package tests proving Kate context handling keeps keywords inside comments and strings from being flattened into global keyword matches, empty input is safe, language mismatch falls back to plain text, and re-highlighting reflects changed text.
- Added a focused Kate XML regression test for block-comment context popping so numbers, keywords, and types inside block comments stay comments while normal Swift rules resume after `*/`.
- Added a latest validation snapshot to the Milestone 2 checklist covering Swift tests, Python tests, build, live smoke, scope cleanliness, syntax tokens, command self-test, and screenshot TCC disposition.
- Added a latest validation snapshot to the Milestone 1 checklist covering Swift tests, Python tests, build, live smoke, scope cleanliness, editor readiness, command self-test, and screenshot TCC disposition.
- Corrected the Milestone 2 App Intents checklist section to list only implemented intents and document status/Clean Text smoke coverage through the actual package and live-editor paths.
- Added a debug-only plain-editor command self-test to the smoke path that exercises insert, Undo, Redo, Select All, Copy, Cut, and Paste against a temporary Swift source file.
- Routed plain-editor menu and command-bar editing commands through the shared active-editor action router so Undo, Redo, Cut, Copy, Paste, and Select All operate on the live `TextView`.
- Implemented the first Clean Text action as deterministic per-line trailing space/tab trimming through the active editor, with smoke coverage for Clean Text, undo, redo, save, and reopen persistence.
- Added narrow App Intents smoke-test hooks plus package validation for opening a known file, reporting state, applying a synthetic edit, saving, reopening, and verifying persistence without display access.
- Added package smoke coverage for Markdown, JSON, YAML, plain-text, and unknown-extension syntax mode detection, with unknown files falling back to plain text.
- Added shared plain-editor status reporting tests for CRLF, CR, LF, UTF-8, UTF-16 LE, soft tabs, tabs, and unknown indentation fallback.
- Saved plain-editor smoke logs under `test-results/plain_editor_smoke/` and asserted UTF-8/LF status reporting in the live smoke path.
- Documented the current non-UTF status-reporting limitation for ambiguous BOM-less files instead of treating encoding detection as complete for every byte pattern.
- Documented Milestone 2's active Find/Replace deferral, Liquid Glass/system-material usage, smoke output location, and status-reporting validation coverage.
- Ignored generated `test-results/` smoke artifacts while keeping the smoke output path documented.
- Removed the remaining terminal-emulator model files from the active SwiftPM executable source list.
- Removed the unused shell client from the active SwiftPM executable path so workspace-shell behavior stays out of scope.
- Pruned unused legacy LSP, extension, database, async, and log package dependencies from the active SwiftPM executable manifest.
- Removed the unused root `SnapshotTesting` dependency from the active package-smoke test target.
- Excluded the unused `World` and timeout helper from the active SwiftPM executable path after shell/LSP cleanup.
- Marked the App Intents smoke-test goal complete with evidence from the package smoke runner and live plain-editor smoke gate, replacing remaining future-oriented smoke-goal wording.
- Tightened the final Milestone 2 checklist evidence for Clean Text now that the command is implemented and smoke-validated.
- Restored the SwiftPM package-smoke lifecycle test file referenced by the root package manifest and added a target-local `CodeEditLanguages` resource placeholder so required builds no longer fail on missing declared inputs.
- Switched the deterministic debug/smoke source file to `CodeFileDocument.swift` so live validation contains comments, keywords, strings, numbers, types, identifiers, and normal text.
- Kept optional screenshot confirmation non-fatal in the plain-editor smoke script when macOS screen-capture permission is unavailable.
- Narrowed the root SwiftPM test target to `CodeEditTests/PackageSmoke` so legacy test files outside the active package-smoke path no longer produce unhandled-file warnings.
- Fixed `docs/FILE_STRUCTURE.md` local links so Markdown link hygiene passes for same-folder docs and generated directories.
- Replaced remaining repo-wide non-ISO decorative glyphs and test fixture names with ASCII-safe equivalents so the Python hygiene suite can run cleanly.
- Explicitly show and order the default document window when the plain-editor file document creates it, restoring visible launch for the smoke path.
- Extended `scripts/plain_editor_smoke.sh` with an optional `~/nsh/easy-screenshot/run.sh --application CodeEdit --preview` confirmation so smoke validation proves a window is actually visible.
- Split the plain-editor work into foundation and product-UI milestones so the remaining behavior gaps stay explicit.
- Added a shared plain-editor action router scaffold and routed the plain-editor commands through the same document action methods used by the app shell.
- Added a `New` command to the plain-editor command group and later replaced the `Clean Text` placeholder with the implemented trailing-whitespace cleanup action.
- Added a visible top command ribbon and bottom status bar scaffold to the plain-editor file view so the app has the intended product shell shape.
- Added provisional status values for cursor position, word count, character count, indentation, encoding, line ending, and syntax mode.
- Added runtime log evidence for the plain-editor command ribbon and status bar so smoke validation can assert the visible shell exists.
- Extended `scripts/plain_editor_smoke.sh` to check the new ribbon and status-bar runtime markers.

## 2026-07-06

### Fixes and Maintenance

- Removed the terminal utility UI test from the active UI-test tree because the milestone excludes terminal behavior.
- Removed the terminal-emulator source tree, the SwiftTerm utility extension, and the Xcode package references so the active repo surface matches the plain-editor scope.
- Added a SwiftPM debug-lane direct-open path that initializes `CodeFileDocument` from a known source URL instead of depending on `NSDocumentController` document registration.
- Logged the live bundle metadata, Swift UTType mapping, and document-controller class list so the plain-editor smoke path can prove the runtime document-type state.
- Made the plain-editor smoke harness start from a clean `CodeEdit` process so it validates a single deterministic launch lane.
- Added a SwiftPM package smoke test target for the `CodeFileDocument` lifecycle path so save/reopen verification runs on the Swift build path.
- Logged the live menu bar during debug launch so the plain-editor smoke run can verify command registration from runtime evidence.
- Captured a live CodeEdit screenshot at `test-results/gui_smoke/editor_window.png` after verifying the app window stayed open long enough for helper-based capture.
- Fixed the plain-editor launch crash by moving `presentedItemDidChange()` back onto the main actor instead of forcing isolated access from the file-presenter queue.
- Moved the plain editor viewport observer back into `CodeEditTextView` and exposed the helper for the app shell.
- Fixed the remaining Swift 6 concurrency blockers in the plain editor viewport observer and shared static helpers.
- Replaced the app entry's welcome-window launcher with a minimal plain-editor scene stub.
- Added a plain-editor launch smoke-run that boots the built executable directly and leaves the app running instead of exiting immediately.
- Added deterministic runtime logs, a plain-editor smoke harness, and a smoke checklist for the file-backed editor path.
- Added a focused CodeFileDocument lifecycle test covering load, synthetic edit, save, and reopen persistence.
- Declared the plain-editor runtime resources explicitly and excluded shell-integration/preview artifacts from the required build path.
- Trimmed the executable manifest to drop the welcome-window package from the app target.
- Added `docs/RELATED_PROJECTS.md` from local repo evidence and bounded sibling-project discovery.
- Replaced the upstream README with a shorter fork-focused front page.
- Linked the README to the docs that already exist in this repository.
- Wired the plain text editor to report text edits directly so the document can mark changes without the removed content-coordinator bridge.
- Trimmed `CodeFileDocument` to use only plain file-path state for file-change detection in this lane.
- Removed the stale `CodeFileView` dependency on the removed content-coordinator subscription.
- Added a root `build.sh` wrapper for `xcodebuild` on the `CodeEdit` scheme.
- Split the build helper into `build_debug.sh` and `build_release.sh` to match the app workflow.
- Added an early Xcode simulator-component check so build scripts fail with a clearer message when Xcode is incomplete.
- Vendored the CodeEdit-owned Swift packages into `Packages/` and rewired the project to use local package paths.
- Replaced the runtime `ZIPFoundation` dependency with a local unzip helper backed by the system `unzip` tool.
- Added a new `CodeEditHighlighting` package skeleton to define the app-facing syntax highlight span model and protocol.
- Wired `CodeEditSourceEditor` to depend on the new shared highlighting package boundary.
- Added a resource-only `CodeEditSyntaxDefinitions` package skeleton for declarative syntax definition files.
- Made the debug and release build scripts interruptible by trapping `INT` and `TERM`.
- Added a syntax-rule-set comparison note to keep the format decision explicit.
- Added a removal plan so cleanup can proceed in a bounded order.
- Added a data-first directory layout for the syntax-definition bundle.
- Added an audit note to bound the remaining cleanup surface.
- Made the keybindings modifier-key environment default immutable so the plain-editor lane stays Swift 6 concurrency-safe.
- Switched `LocalizedStringKey.helloWorld` to a computed value so the localization helper stays Swift 6 concurrency-safe.
- Removed the default parser-backed provider from the editor entry points.
- Reduced `CodeEditLanguages` to metadata-only source files in the package target.
- Removed parser references from the source editor package docs and comments.
- Removed Sparkle and SwiftLintPlugin from the required build graph and stubbed the updater entry point.
- Switched the debug and release helper scripts from Xcode build invocation to SwiftPM build invocation.
- Continued the SwiftPM-first, source-first refactor toward a smaller editor/highlighting boundary.
- Added `docs/CODE_ARCHITECTURE.md` and `docs/FILE_STRUCTURE.md` to explain the plain-editor cutover and repo layout.
- Removed `SwiftTerm` from the executable dependency graph because the milestone scope excludes a built-in terminal.
- Continued cutting the executable target away from the legacy workspace shell so the app can boot through the plain editor path.
